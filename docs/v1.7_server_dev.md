# v1.7 后端（server）开发文档

> **版本**：v1.7
> **日期**：2026-06-18
> **基线**：PRD v1.7（用户已确认默认值）+ 架构师评审 `CONDITIONAL APPROVED 8.62`
> **目标读者**：后端开发 Agent
> **状态**：开发文档草案，待后端开发 Agent 评审

---

## 1. 概述

本次后端开发范围覆盖 v1.7 的 P0 主链路：

1. **小程序码系统**：商品、分类、活动、CMS 自定义页面生成小程序码，扫码解析与归因日志。
2. **CMS 页面二维码**：复用 v1.6 `CmsPage`，为已发布页面生成二维码。
3. **订单高级筛选与导出**：扩展订单查询参数，提供私有导出任务和鉴权下载。
4. **资金结算**：支付成功进入 `pending_amount`，订单完成后结算到 `available_amount`。
5. **退款策略升级**：新增 RefundRecord 主事实表，支持全额/部分/按项退款，支持保留订单或取消订单。

**不在本端范围内**：

- 管理后台页面与交互实现。
- 小程序扫码分发页与本地归因存储。
- 商家小程序版后台 P2 首期实现。

---

## 2. 文件清单

### 2.1 新增文件

```text
server/
├── models/qrcode.py                  # 小程序码与扫码日志模型
├── models/refund.py                  # 退款主记录与退款明细
├── models/settlement.py              # 订单结算记录
├── models/export_task.py             # 订单导出任务
├── schemas/qrcode.py                 # 小程序码请求/响应 schema
├── schemas/refund.py                 # 新退款 API schema
├── schemas/settlement.py             # 结算记录 schema
├── schemas/order_export.py           # 订单导出 schema
├── routers/qrcodes.py                # C/B 端二维码 API
├── routers/refunds.py                # 新退款 API
├── services/qrcode_service.py        # 微信小程序码生成、缓存、解析
├── services/refund_service.py        # 退款状态机与微信退款编排
├── services/settlement_service.py    # 资金结算与补偿逻辑
├── services/order_export_service.py  # 订单筛选导出任务
├── tasks/settlement.py               # completed 订单结算补偿
└── tasks/order_export.py             # 导出任务生成与过期文件清理
```

### 2.2 修改文件

| 文件 | 变更说明 |
|------|----------|
| `models/__init__.py` | 导入 MiniProgramQRCode、RefundRecord、FinanceSettlement、OrderExportTask 等模型 |
| `models/order.py` | 新增来源、结算、退款状态字段 |
| `models/finance.py` | `TransactionType` 增加 `settlement`，`FinanceTransaction` 关联 refund_record_id |
| `models/admin.py` | 权限资源增加 `qrcode`、`order_export`、可选 `refund` |
| `schemas/order.py` | 扩展订单创建来源字段、列表筛选字段、响应字段 |
| `schemas/finance.py` | 扩展 overview、transaction、settlement 响应 |
| `routers/orders.py` | 高级筛选参数、订单导出入口、订单来源写入 |
| `routers/finance.py` | 增加结算记录列表和手动结算接口 |
| `routers/cms.py` | 增加 `/admin/cms/pages/{id}/qrcode` |
| `routers/payments.py` | 支付/退款通知接入 Finance/RefundRecord 幂等更新 |
| `services/order_service.py` | 订单列表筛选、来源归因、完成后触发结算 |
| `services/finance_service.py` | 行级锁账户更新、结算流水、退款扣减 |
| `services/wechat_pay_service.py` | 小程序码 access_token 缓存、退款查询补偿（如未已有） |
| `main.py` | 注册 qrcodes、refunds 路由 |
| `celery_app.py` / `celery_config.py` | 注册 settlement/export 任务 |

---

## 3. 数据模型

### 3.1 小程序码

```python
class MiniProgramQRCode(Base):
    __tablename__ = "mini_program_qrcode"
    __table_args__ = (
        Index(
            "uq_qrcode_site_target_channel_scene",
            "site_id", "target_type", "target_key", "channel", "scene",
            unique=True,
            postgresql_where=text("is_deleted = false"),
        ),
        Index("idx_qrcode_short_code", "short_code", unique=True),
        Index("idx_qrcode_site_status", "site_id", "status"),
    )

    site_id = mapped_column(BigInteger, nullable=False, default=1, server_default="1")
    target_type = mapped_column(String(32), nullable=False)
    target_key = mapped_column(String(128), nullable=False)
    title = mapped_column(String(128), nullable=False)
    path = mapped_column(String(256), nullable=False)
    scene = mapped_column(String(128), nullable=False)
    short_code = mapped_column(String(32), nullable=False, unique=True)
    channel = mapped_column(String(64), nullable=False, default="default")
    image_url = mapped_column(String(512), nullable=True)
    status = mapped_column(String(16), nullable=False, default="active")
    generated_by = mapped_column(BigInteger, nullable=True)
    generated_at = mapped_column(DateTime(timezone=True), nullable=True)
    last_used_at = mapped_column(DateTime(timezone=True), nullable=True)
    usage_count = mapped_column(Integer, nullable=False, default=0, server_default="0")
```

```python
class MiniProgramQRCodeScanLog(Base):
    __tablename__ = "mini_program_qrcode_scan_log"
    __table_args__ = (
        Index("idx_qrcode_scan_qr_time", "qr_code_id", "scanned_at"),
        Index("idx_qrcode_scan_site_time", "site_id", "scanned_at"),
    )

    site_id = mapped_column(BigInteger, nullable=False)
    qr_code_id = mapped_column(BigInteger, ForeignKey("mini_program_qrcode.id"), nullable=False)
    user_id = mapped_column(BigInteger, nullable=True)
    openid = mapped_column(String(64), nullable=True)
    channel = mapped_column(String(64), nullable=True)
    scanned_at = mapped_column(DateTime(timezone=True), nullable=False)
    raw_scene = mapped_column(String(256), nullable=True)
    client_info = mapped_column(JSONB, nullable=True)
```

### 3.2 订单来源与结算字段

| 表 | 字段 | 说明 |
|----|------|------|
| `Order` | `source_qrcode_id` BigInteger nullable | 来源小程序码 |
| `Order` | `source_channel` String(64) nullable | 来源渠道 |
| `Order` | `source_scanned_at` DateTime nullable | 归因扫码时间 |
| `Order` | `settled_amount` Numeric(10,2) default 0 | 累计已结算金额 |
| `Order` | `settlement_status` String(20) default `unsettled` | unsettled/partial/settled/failed |
| `Order` | `refund_status` String(20) default `none` | none/pending/partial/refunded/rejected |
| `OrderItem` | `refund_status` 扩展 | none/pending/partial/refunded |

### 3.3 资金结算

```python
class FinanceSettlement(Base):
    __tablename__ = "finance_settlement"
    __table_args__ = (
        UniqueConstraint("order_id", "settlement_no", name="uq_settlement_order_no"),
        Index("idx_settlement_site_status", "site_id", "status"),
        Index("idx_settlement_order", "order_id"),
    )

    site_id = mapped_column(BigInteger, nullable=False)
    order_id = mapped_column(BigInteger, nullable=False)
    settlement_no = mapped_column(String(40), nullable=False)
    amount = mapped_column(Numeric(10, 2), nullable=False)
    status = mapped_column(String(20), nullable=False, default="completed")
    trigger_type = mapped_column(String(32), nullable=False)  # order_completed/celery_compensation/manual_retry
    settled_at = mapped_column(DateTime(timezone=True), nullable=True)
    error_message = mapped_column(String(500), nullable=True)
```

迁移必须增加账户非负约束：

```sql
ALTER TABLE finance_account
ADD CONSTRAINT ck_finance_pending_non_negative CHECK (pending_amount >= 0),
ADD CONSTRAINT ck_finance_available_non_negative CHECK (available_amount >= 0);
```

### 3.4 退款主事实表

```python
class RefundRecord(Base):
    __tablename__ = "refund_record"
    __table_args__ = (
        Index("idx_refund_site_status", "site_id", "status"),
        Index("idx_refund_order", "order_id"),
        Index("idx_refund_no", "refund_no", unique=True),
    )

    site_id = mapped_column(BigInteger, nullable=False)
    order_id = mapped_column(BigInteger, nullable=False)
    refund_no = mapped_column(String(40), nullable=False, unique=True)
    refund_mode = mapped_column(String(20), nullable=False)  # full/partial/item
    order_action = mapped_column(String(20), nullable=False)  # keep_order/cancel_order
    refund_amount = mapped_column(Numeric(10, 2), nullable=False)
    system_amount = mapped_column(Numeric(10, 2), nullable=False)
    release_inventory = mapped_column(Boolean, nullable=False)
    reason = mapped_column(String(500), nullable=False)
    risk_level = mapped_column(String(20), nullable=False, default="normal")
    status = mapped_column(String(20), nullable=False, default="pending")
    wechat_refund_id = mapped_column(String(64), nullable=True)
    requested_by = mapped_column(BigInteger, nullable=False)
    approved_by = mapped_column(BigInteger, nullable=True)
    approved_at = mapped_column(DateTime(timezone=True), nullable=True)
    completed_at = mapped_column(DateTime(timezone=True), nullable=True)
```

```python
class RefundRecordItem(Base):
    __tablename__ = "refund_record_item"

    refund_record_id = mapped_column(BigInteger, ForeignKey("refund_record.id"), nullable=False)
    order_item_id = mapped_column(BigInteger, nullable=False)
    refund_amount = mapped_column(Numeric(10, 2), nullable=False)
    quantity = mapped_column(Integer, nullable=False, default=1)
    release_inventory = mapped_column(Boolean, nullable=False, default=True)
```

### 3.5 导出任务

```python
class OrderExportTask(Base):
    __tablename__ = "order_export_task"
    __table_args__ = (
        Index("idx_order_export_site_status", "site_id", "status"),
        Index("idx_order_export_creator", "created_by"),
    )

    site_id = mapped_column(BigInteger, nullable=False)
    task_no = mapped_column(String(40), nullable=False, unique=True)
    filters = mapped_column(JSONB, nullable=False)
    file_format = mapped_column(String(10), nullable=False, default="xlsx")
    file_path = mapped_column(String(512), nullable=True)  # 私有路径，不是公开 URL
    row_count = mapped_column(Integer, nullable=True)
    status = mapped_column(String(20), nullable=False, default="pending")
    error_message = mapped_column(String(500), nullable=True)
    created_by = mapped_column(BigInteger, nullable=False)
    completed_at = mapped_column(DateTime(timezone=True), nullable=True)
    expires_at = mapped_column(DateTime(timezone=True), nullable=True)
```

---

## 4. API 设计

### 4.1 小程序码

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `POST` | `/api/v1/admin/qrcodes` | 创建或复用小程序码 | admin write |
| `GET` | `/api/v1/admin/qrcodes` | 二维码列表 | admin read |
| `GET` | `/api/v1/admin/qrcodes/{id}` | 二维码详情 | admin read |
| `POST` | `/api/v1/admin/qrcodes/{id}/regenerate` | 重新生成 | admin write |
| `PATCH` | `/api/v1/admin/qrcodes/{id}/status` | active/inactive | super_admin |
| `GET` | `/api/v1/admin/qrcodes/{id}/download` | 下载图片 | admin read |
| `POST` | `/api/v1/admin/cms/pages/{id}/qrcode` | CMS 页面二维码 | admin write |
| `GET` | `/api/v1/qrcodes/resolve` | C 端扫码解析 | public |

`POST /admin/qrcodes` 请求：

```json
{
  "target_type": "product",
  "target_key": "123",
  "title": "湖畔营位 A 区",
  "channel": "poster"
}
```

解析响应：

```json
{
  "qr_code_id": 12,
  "target_type": "product",
  "target_key": "123",
  "title": "湖畔营位 A 区",
  "path": "/pages/product-detail/index?id=123",
  "channel": "poster",
  "status": "active",
  "attribution_ttl_seconds": 86400
}
```

**安全硬约束**：

- `path` 只能由后端枚举模板生成，不允许从数据库任意字符串直接返回。
- 微信返回必须校验 `Content-Type=image/*` 和文件大小；错误 JSON 不得保存为图片。
- access_token 必须缓存，限流/网络错误可重试，参数错误直接失败。

### 4.2 订单高级筛选与导出

扩展 `GET /api/v1/admin/orders` 查询参数：

```text
keyword,status,payment_status,refund_status,order_type,category,product_id,sku_id,
qrcode_id,channel,created_start,created_end,paid_start,paid_end,
booking_start,booking_end,amount_min,amount_max,user_id,user_phone,verify_status,
sort_by,sort_order,page,page_size
```

新增导出 API：

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/admin/orders/export` | 创建导出任务或同步生成小文件 |
| `GET` | `/api/v1/admin/orders/export-tasks` | 导出任务列表 |
| `GET` | `/api/v1/admin/orders/export-tasks/{id}` | 任务详情 |
| `GET` | `/api/v1/admin/orders/export-tasks/{id}/download` | 鉴权下载 |

导出文件必须写入私有目录，例如 `server/private_exports/orders/`。下载只能通过认证接口流式返回，不得放入 `/images` 等公开静态目录。

### 4.3 资金结算

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/admin/finance/settlements` | 结算记录列表 |
| `POST` | `/api/v1/admin/orders/{id}/settle` | 手动触发结算，super_admin |
| `GET` | `/api/v1/admin/finance/overview` | 增加结算统计 |

支付成功处理：

1. 锁定 `Order`。
2. 以微信 `transaction_id/payment_no` 幂等检查。
3. `FinanceAccount.pending_amount += order.actual_amount`。
4. 创建 `FinanceTransaction(type='income', account_type='pending')`。

结算处理：

1. 仅处理 `Order.status='completed'` 且 `settlement_status != 'settled'`。
2. 同一事务内 `SELECT ... FOR UPDATE` 锁定 `Order` 与 `FinanceAccount`。
3. `settle_amount = actual_amount - refunded_amount - settled_amount`。
4. `pending_amount -= settle_amount`，`available_amount += settle_amount`。
5. 创建 `FinanceTransaction(type='settlement', from_account='pending', to_account='available')`。

### 4.4 新退款 API

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/admin/orders/{id}/refunds` | 创建退款操作 |
| `GET` | `/api/v1/admin/orders/{id}/refunds` | 订单退款记录 |
| `GET` | `/api/v1/admin/refunds` | 退款列表/审批队列 |
| `GET` | `/api/v1/admin/refunds/{id}` | 退款详情 |
| `POST` | `/api/v1/admin/refunds/{id}/approve` | 审批通过 |
| `POST` | `/api/v1/admin/refunds/{id}/reject` | 拒绝 |

创建请求：

```json
{
  "refund_mode": "partial",
  "order_action": "keep_order",
  "refund_amount": "50.00",
  "release_inventory": false,
  "reason": "服务体验补偿",
  "items": [
    { "order_item_id": 1001, "refund_amount": "50.00", "quantity": 1 }
  ],
  "confirm_code": "123456"
}
```

状态分层规则：

| 场景 | `Order.status` | `payment_status` | `refund_status` |
|------|----------------|------------------|-----------------|
| 部分退款不取消 | 不改变服务状态 | partial_refunded | partial |
| 全额退款不取消 | 不改变服务状态 | refunded | refunded |
| 全额退款并取消 | cancelled | refunded | refunded |
| 部分退款并取消 | cancelled | partial_refunded | partial |

全额退款不取消订单只能 `super_admin` 操作，必须二次确认并强制备注。

---

## 5. 服务层关键约束

### 5.1 二维码归因

- 小程序扫码解析成功后会在下单时传 `source_qrcode_id/source_channel/source_scanned_at`。
- 后端必须校验二维码存在、active、site_id 匹配。
- 归因 TTL 默认 24 小时，超过 TTL 的来源不写入订单。

### 5.2 导出任务

- 5000 行以内可同步生成任务并返回 completed。
- 超过 5000 行写入 pending，由 Celery 生成。
- 导出字段默认手机号脱敏；完整手机号需要 `order export` 权限。
- `OperationLog` 记录 filters、row_count、file_format、created_by。

### 5.3 退款与财务

- 未结算订单退款优先扣 `pending_amount`。
- 已结算订单退款扣 `available_amount`；不足时审批失败。
- 部分结算订单优先扣 pending，不足扣 available。
- 微信退款成功回调以 `out_refund_no/refund_no` 幂等更新 `RefundRecord`。
- 回调未到的 processing 记录由定时任务查询补偿。

---

## 6. 验证命令

```bash
cd /Users/nathan/Projects/yyyl/server
/Users/nathan/miniconda3/envs/yyyl/bin/python -m unittest \
  tests/test_qrcode_service.py \
  tests/test_order_export_service.py \
  tests/test_settlement_service.py \
  tests/test_refund_service.py \
  tests/test_order_routes.py \
  tests/test_payment_routes.py -v
```

路由注册检查：

```bash
cd /Users/nathan/Projects/yyyl/server
/Users/nathan/miniconda3/envs/yyyl/bin/python -c "from main import app; print([r.path for r in app.routes if 'qrcodes' in r.path or 'refunds' in r.path])"
```

---

## 7. 验收标准

- [ ] 商品、分类、活动、CMS 页面均可生成并复用小程序码。
- [ ] 二维码解析只返回白名单内部路径。
- [ ] 下单可写入二维码来源，订单导出可按来源筛选。
- [ ] 订单导出文件只能通过后台鉴权接口下载。
- [ ] 支付成功只入账一次到不可提现账户。
- [ ] completed 订单只结算一次到可提现账户。
- [ ] 退款可全额/部分/按项，并可 keep_order/cancel_order。
- [ ] 退款与结算并发不会导致账户负数或重复流水。
