# v1.7 小程序端开发文档评审报告

> **评审对象**：`docs/v1.7_miniapp_dev.html` / `docs/v1.7_miniapp_dev.md`
> **评审日期**：2026-06-18
> **评审角色**：小程序开发 Agent
> **评审结论**：APPROVED
> **加权总分**：8.74 / 10

---

## 1. 评分明细

| 维度 | 权重 | 得分 | 加权分 | 评价 |
|------|------|------|--------|------|
| API 接口完整性 | 25% | 8.7 | 2.175 | 解析 API 与下单来源字段覆盖完整 |
| 数据模型可执行性 | 20% | 8.6 | 1.72 | qrcode/attribution 类型清晰，订单创建扩展明确 |
| 技术方案兼容性 | 25% | 8.9 | 2.225 | 与 uni-app pages、tabBar、CMS 页面容器兼容 |
| 业务规则严密性 | 15% | 8.8 | 1.32 | 覆盖扫码停用、白名单路径、24h TTL、分类 tabBar 特殊跳转 |
| 可开发性 | 15% | 8.7 | 1.305 | 文件清单、核心代码、手工验证路径明确 |
| **总分** | **100%** | — | **8.745** | **8.74 / 10** |

---

## 2. 结论

小程序开发文档达到可开发标准。文档正确识别了 `pages/category/index` 是 tabBar 页面，不能用 `redirectTo`，并给出了 storage + `switchTab` 的可执行方案。扫码归因 TTL、下单携带来源、CMS 页面错误态也覆盖完整。

## 3. 实现阶段注意

| 编号 | 要求 |
|------|------|
| MP-01 | `scene` 必须 `decodeURIComponent`，同时兼容 `scene` 和 `q` 参数 |
| MP-02 | 跳转 path 必须再次做小程序端白名单校验 |
| MP-03 | 分类页读取 pending category 后要清除 storage，避免后续误筛选 |
| MP-04 | 归因信息过期后必须自动清除 |
| MP-05 | 下单接口失败时不要清除归因，避免用户重试丢来源 |

---

## 4. 验收建议

```bash
cd /Users/nathan/Projects/yyyl/uni-app
npm run type-check
npm run build:wx:xijiao
npm run build:wx:dalonggu
```
