# v1.7 实施计划

> **版本**：v1.7
> **日期**：2026-06-18
> **输入文档**：PRD、架构评审、三端开发文档与三端评审报告
> **状态**：实现完成，已验证

---

## 1. 总览

v1.7 采用“后端基础能力优先、薄垂直切片集成”的实施方式。先建立共享数据模型和 API 契约，再按二维码、订单导出、资金结算、退款、小程序归因、Admin 管理界面分批开发。每个阶段必须保持可构建、可测试、可回滚。

---

## 2. 阶段与任务

### Phase 1：后端基础模型与契约

#### Task 1：新增 v1.7 数据模型与迁移 ✅ 已完成

**范围**：新增 qrcode/refund/settlement/export_task 模型，扩展 order/finance/admin 权限字段。

**验收**：

- [x] Alembic 迁移包含新增表、索引、唯一约束、账户非负约束。
- [x] `models/__init__.py` 导入新模型。
- [x] 迁移可 upgrade/downgrade。

**验证**：

```bash
cd server
/Users/nathan/miniconda3/envs/yyyl/bin/python -m unittest tests/test_model_imports.py -v
```

**依赖**：无。

#### Task 2：定义后端 schema 与路由空壳 ✅ 已完成

**范围**：新增 qrcode/refund/settlement/order_export schema 与路由注册，先返回明确的未实现或最小结构。

**验收**：

- [x] FastAPI 路由注册成功。
- [x] 请求/响应 schema 可被 OpenAPI 加载。
- [x] 不影响现有订单/支付路由。

**验证**：

```bash
cd server
/Users/nathan/miniconda3/envs/yyyl/bin/python -c "from main import app; print(len(app.routes))"
```

**依赖**：Task 1。

### Checkpoint 1：后端契约

- [x] 后端导入无异常。
- [x] 新路由可在 app.routes 中看到。
- [x] 迁移结构经过人工检查。

---

### Phase 2：小程序码与扫码归因

#### Task 3：实现小程序码生成与解析服务 ✅ 已完成

**范围**：`qrcode_service.py`、`routers/qrcodes.py`，含 access_token 缓存、微信接口错误处理、path 白名单。

**验收**：

- [x] 商品/分类/活动/CMS 页面可创建或复用二维码。
- [x] `GET /qrcodes/resolve` 返回白名单内部路径。
- [x] 停用二维码无法跳转。

**验证**：

```bash
cd server
/Users/nathan/miniconda3/envs/yyyl/bin/python -m unittest tests/test_qrcode_service.py -v
```

**实现记录**：

- 新增 `server/services/qrcode_service.py`，实现目标路径白名单、短码生成、微信小程序 access_token 缓存、小程序码图片生成、二维码复用、扫码解析与扫码日志。
- `server/routers/qrcodes.py` 已接入真实创建、列表、详情、重新生成、状态更新、下载和解析逻辑。
- `server/routers/cms.py` 的 `/admin/cms/pages/{page_id}/qrcode` 已接入已发布 CMS 页面二维码生成逻辑。
- 新增 `server/tests/test_qrcode_service.py` 覆盖路径白名单、复用、CMS 未发布拦截、解析统计、停用拒绝、微信图片校验和多营地配置。

**依赖**：Task 1、Task 2。

#### Task 4：小程序扫码分发页与归因缓存 ✅ 已完成

**范围**：`pages/qr/landing.vue`、`api/qrcode.ts`、`utils/attribution.ts`、`pages/category/index.vue`。

**验收**：

- [x] scene 解析成功后按目标类型跳转。
- [x] 分类 tab 使用 storage + switchTab 正确筛选。
- [x] 下单请求携带有效来源。

**验证**：

```bash
cd uni-app
npm run type-check
```

**实现记录**：

- 新增 `uni-app/src/pages/qr/landing.vue`，解析 `scene` 后调用后端 `/qrcodes/resolve`，保存来源归因并按内部白名单路径跳转。
- 新增 `uni-app/src/api/qrcode.ts`、`uni-app/src/types/qrcode.ts`、`uni-app/src/utils/attribution.ts`，统一二维码解析类型、归因 TTL、本地 storage 和分类待跳转参数。
- `uni-app/src/pages/category/index.vue` 支持从 storage 消费待选分类，适配 `switchTab` 不支持 query 的限制。
- `uni-app/src/pages/order-confirm/index.vue` 在创建订单时携带 `source_qrcode_id/source_channel/source_scanned_at`。
- 后端 `order_service.create_order` 与订单路由已接收并写入来源字段，避免前端来源字段丢失。

**依赖**：Task 3。

#### Task 5：Admin 二维码管理与业务入口 ✅ 已完成

**范围**：`views/qrcode/index.vue`、二维码弹窗、商品/CMS 页面入口。

**验收**：

- [x] 已发布 CMS 页面可生成二维码，未发布禁用。
- [x] 二维码可预览、下载、停用、重新生成。
- [x] 商品/活动/分类入口可生成对应二维码。

**验证**：

```bash
cd admin
npm run build
```

**实现记录**：

- 新增 `admin/src/api/qrcode.ts`、`admin/src/types/qrcode.ts`、`admin/src/views/qrcodes/index.vue`，覆盖二维码列表筛选、手动生成、预览、下载、启停和重新生成。
- `admin/src/router/index.ts` 新增 `/qrcodes` 二维码管理入口。
- `admin/src/views/cms/index.vue` 新增已发布且启用页面的“二维码”快捷入口，未发布/停用页面按钮禁用。
- `admin/src/views/products/index.vue` 新增商品二维码快捷生成入口，按商品类型自动选择 `product/activity_product`。
- 验证：`cd admin && npm run build` 已通过。期间顺手修复 CMS 属性面板、CMS props、CMS store、营地/日历/仪表盘/费用统计/绩效等历史 TypeScript 严格类型错误，解除全量构建阻塞。

**依赖**：Task 3。

### Checkpoint 2：二维码端到端

- [x] 后端二维码测试通过。
- [x] 小程序扫码分发类型检查通过。
- [x] Admin 二维码管理代码已实现，Admin 全量构建通过。

---

### Phase 3：订单高级筛选与导出

#### Task 6：后端订单高级筛选 ✅ 已完成

**范围**：扩展 `OrderListParams`、`order_service.list_orders`，支持 PRD 筛选项和来源字段。

**验收**：

- [x] 商品、分类、预定日期、支付时间、金额、核销状态、二维码渠道可筛选。
- [x] 关键词支持订单号、用户昵称、手机号、商品名。

**验证**：

```bash
cd server
/Users/nathan/miniconda3/envs/yyyl/bin/python -m unittest tests/test_order_filters.py -v
```

**实现记录**：

- 新增 `order_service.build_order_list_query()`，统一构造列表/导出可复用筛选条件。
- `OrderListParams` 扩展商品、品类、预定日期、支付时间、金额区间、核销状态、二维码渠道筛选。
- 管理端订单列表已接入新增筛选参数，Admin 订单页增加对应筛选控件。
- 新增 `server/tests/test_order_filters.py` 覆盖 SQL 查询条件和关键词跨订单号/用户/手机号/商品名搜索。

**依赖**：Task 1。

#### Task 7：后端订单导出任务 ✅ 已完成

**范围**：`order_export_service.py`、私有文件目录、导出任务 API、Celery 清理任务。

**验收**：

- [x] 5000 行以内可同步完成，超过转异步。
- [x] 下载必须鉴权，文件不在公开目录。
- [x] 操作日志记录 filters 和 row_count。

**验证**：

```bash
cd server
/Users/nathan/miniconda3/envs/yyyl/bin/python -m unittest tests/test_order_export_service.py -v
```

**实现记录**：

- 新增 `server/services/order_export_service.py`，导出文件落在 `server/private/exports/orders/{site_id}`，不挂载公开静态目录。
- 5000 行以内同步生成 CSV 文件；超过阈值保留 `pending` 任务，供后续 Celery 异步任务接管。
- `routers/orders.py` 的导出任务创建、列表、详情、下载接口已接入真实服务。
- 导出完成写入 `OperationLog`，包含 `filters`、`row_count`、`task_no`、`file_format`。
- 新增 `server/tests/test_order_export_service.py` 覆盖私有文件、任务状态、下载校验和操作日志。

**依赖**：Task 6。

#### Task 8：Admin 高级筛选与导出任务抽屉 ✅ 已完成

**范围**：订单列表筛选组件、导出弹窗、任务抽屉、blob 下载。

**验收**：

- [x] 筛选条件与列表请求一致。
- [x] 导出使用当前筛选条件。
- [x] 下载走 blob 鉴权接口。

**验证**：

```bash
cd admin
npm run build
```

**实现记录**：

- `admin/src/views/orders/index.vue` 已增加商品、品类、预定日期、支付时间、金额、核销状态、二维码渠道等高级筛选项。
- 新增订单导出弹窗，导出请求使用当前筛选条件。
- 新增导出任务抽屉，支持任务列表刷新和 completed 任务 blob 下载。
- 新增 `admin/src/types/order-export.ts`，`admin/src/api/order.ts` 接入导出任务创建、列表和下载接口。
- 验证：`cd admin && npm run build` 已通过。订单高级筛选、导出任务抽屉和 blob 下载参与全量类型检查与生产构建。

**依赖**：Task 6、Task 7。

### Checkpoint 3：订单导出

- [x] 订单筛选测试通过。
- [x] 导出文件权限和脱敏经过服务测试覆盖。
- [x] Admin 筛选和导出流程代码已实现，Admin 全量构建通过。

---

### Phase 4：资金结算

#### Task 9：支付成功 pending 入账 ✅ 已完成

**范围**：支付回调、模拟支付、FinanceTransaction 幂等。

**验收**：

- [x] 微信回调重复不会重复入账。
- [x] 支付成功进入 `pending_amount`。
- [x] 年卡/次数卡/积分兑换不进入 pending。

**验证**：

```bash
cd server
/Users/nathan/miniconda3/envs/yyyl/bin/python -m unittest tests/test_payment_routes.py tests/test_settlement_service.py -v
```

**实现记录**：

- 新增 `server/services/settlement_service.py`，支付成功时以 `order_id + site_id + income/pending` 查询幂等流水。
- 现金类支付方式 `wechat_pay/mock_pay` 进入 `FinanceAccount.pending_amount`，同步增加 `total_income`，非现金方式跳过。
- `order_service.mark_order_paid()` 已调用 `record_payment_pending_income()`，微信回调和模拟支付共用同一入口。
- 新增 `server/tests/test_settlement_service.py` 覆盖入账、幂等、非现金跳过。

**依赖**：Task 1。

#### Task 10：订单 completed 自动结算 ✅ 已完成

**状态**：已完成。

**范围**：`settlement_service.py`、手动结算 API、Celery 补偿任务。

**验收**：

- [x] completed 订单从 pending 转 available。
- [x] 重复完成/重复补偿不重复结算。
- [x] pending 不足时失败且不扣成负数。

**验证**：

```bash
cd server
/Users/nathan/miniconda3/envs/yyyl/bin/python -m unittest tests/test_settlement_service.py -v
```

**依赖**：Task 9。

#### Task 11：Admin 财务结算展示 ✅ 已完成

**状态**：已完成。

**范围**：财务概览、结算记录页、订单详情结算状态。

**验收**：

- [x] 可查看 pending/available/settlement 统计。
- [x] 可查看结算记录和失败原因。
- [x] super_admin 可手动重试失败结算。

**验证**：

```bash
cd admin
npm run build
```

**依赖**：Task 10。

### Checkpoint 4：资金闭环

- [x] 支付入账、订单完成、自动结算、手动重试都可验证。
- [x] 账户余额无负数。

---

### Phase 5：退款策略升级

#### Task 12：后端 RefundRecord 主流程 ✅ 已完成

**状态**：已完成。

**范围**：新退款 API、RefundRecord/Item、全额/部分/按项退款、keep_order/cancel_order 状态机。

**验收**：

- [x] 退款创建记录完整。
- [x] keep_order 不改变服务状态。
- [x] cancel_order 作废电子票并按规则释放库存。

**验证**：

```bash
cd server
/Users/nathan/miniconda3/envs/yyyl/bin/python -m unittest tests/test_refund_service.py -v
```

**依赖**：Task 1、Task 9。

#### Task 13：微信退款回调与补偿 ✅ 已完成

**状态**：已完成。

**范围**：退款申请、退款通知、主动查询补偿、Finance 扣减。

**验收**：

- [x] 微信退款成功回调幂等更新 RefundRecord。
- [x] 未结算订单从 pending 扣，已结算订单从 available 扣。
- [x] available 不足时审批失败。

**验证**：

```bash
cd server
/Users/nathan/miniconda3/envs/yyyl/bin/python -m unittest tests/test_refund_service.py tests/test_payment_routes.py -v
```

**依赖**：Task 10、Task 12。

#### Task 14：Admin 统一退款弹窗与退款历史 ✅ 已完成

**状态**：已完成。

**范围**：订单详情退款弹窗、退款历史、退款审批/拒绝，高危确认。

**验收**：

- [x] full/partial/item 三种模式可提交。
- [x] keep_order/cancel_order 默认库存策略正确。
- [x] 全额退款不取消订单强确认。

**验证**：

```bash
cd admin
npm run build
```

**依赖**：Task 12、Task 13。

### Checkpoint 5：退款闭环

- [x] 微信退款、订单状态、库存、电子票、资金账户一致。
- [x] Admin 高危操作权限正确。

---

## 3. 最终验收

```bash
cd server
/Users/nathan/miniconda3/envs/yyyl/bin/python -m unittest \
  tests/test_qrcode_service.py \
  tests/test_order_filters.py \
  tests/test_order_export_service.py \
  tests/test_settlement_service.py \
  tests/test_refund_service.py \
  tests/test_payment_routes.py \
  tests/test_v17_contracts.py \
  tests/test_order_routes.py -v

cd ../admin
npm run build

cd ../uni-app
npm run type-check
npm run build:wx:xijiao
npm run build:wx:dalonggu
```

**2026-06-18 验证记录**：

- [x] 后端 v1.7 回归：35 tests passed。
- [x] Admin：`npm run build` passed，仅保留 Vite 大 chunk 警告。
- [x] 小程序：`npm run type-check` passed。
- [x] 小程序构建：`npm run build:wx:xijiao`、`npm run build:wx:dalonggu` passed，仅保留 uni-app/Sass 既有弃用警告。

---

## 4. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 资金并发导致余额错误 | 高 | 行级锁、唯一约束、账户非负约束、幂等流水 |
| 退款状态影响旧订单列表 | 高 | 状态分层，旧接口兼容层只做桥接 |
| 导出泄露敏感数据 | 高 | 私有目录、鉴权下载、手机号默认脱敏 |
| 小程序码微信接口限流 | 中 | access_token 缓存、按错误码重试、批量生成异步化 |
| 分类 tabBar 参数传递失效 | 中 | storage + switchTab + onShow 消费 |

---

## 5. 上线前检查

v1.7 已完成实现与本地验证。进入发布前，按以下顺序做生产前检查：

- 审阅并执行 Alembic 迁移，确认新增表、索引、唯一约束和账户非负约束符合生产数据库状态。
- 配置微信小程序码生成所需的各营地 AppID/AppSecret，确认二维码图片目录可写且不暴露私有导出目录。
- 发布后优先验证二维码解析、订单导出下载、支付入账、订单完成结算、退款回调五条闭环。
