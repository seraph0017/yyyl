# v1.6 小程序端（uni-app）开发文档

> **版本**：v1.6
> **日期**：2026-03-31
> **基线**：PRD v1.6-rev1 增量需求 — 需求 1「小程序可视化装修系统」C端渲染部分
> **目标读者**：uni-app 小程序端开发者

---

## 1. 概述

本次小程序端开发范围为 **CMS 动态页面渲染引擎**，核心工作包括：

1. **CMS 渲染引擎**：构建一套基于 Vue3 动态组件（`<component :is>`）的通用渲染引擎，接收后端下发的页面配置 JSON，按组件列表依次渲染 11 种组件类型
2. **11 种 CMS 组件**：Banner 轮播图、图片区块、图文卡片、公告栏、快捷导航、商品列表、优惠券区块、富文本、间距、分割线、视频区块
3. **超链接跳转工具**：统一处理 7 种链接类型的跳转逻辑
4. **首页改造**：将现有硬编码首页改造为 CMS 动态渲染模式，保留三级降级方案
5. **CMS 通用页面容器**：新增独立页面，通过 `page_code` 参数加载任意 CMS 配置页面（活动页、促销页等）
6. **预览模式**：支持通过 `preview_token` 参数预览草稿内容

**不在本次小程序端开发范围内**：
- 需求 2「企业宣传首页」仅影响管理后台，小程序端无变更
- 后端 API 开发（由后端团队负责）
- 管理后台画板编辑器（由 Admin 端团队负责）

---

## 2. 页面路由

### 2.1 pages.json 新增页面

在 `pages` 数组中新增 CMS 通用页面容器：

> **M05 分包建议**：`pages/cms-page/index` 不是 tabBar 页面，建议放入 `subPackages` 以减小主包体积。CMS 组件因首页依赖必须在主包中，但 CMS 通用页面容器可分包加载。如果主包体积接近 2MB 限制，按以下方式调整：
>
> ```json
> // pages.json subPackages 配置
> {
>   "subPackages": [{
>     "root": "pages-sub/cms",
>     "pages": [
>       { "path": "cms-page/index", "style": { "navigationBarTitleText": "活动", "enablePullDownRefresh": true } }
>     ]
>   }]
> }
> ```
> 此时 `handleActivityLink` 中的跳转路径需同步修改为 `/pages-sub/cms/cms-page/index`。

如暂不分包，在主包 `pages` 数组中添加：

```json
{
  "path": "pages/cms-page/index",
  "style": {
    "navigationBarTitleText": "活动",
    "enablePullDownRefresh": true
  }
}
```

> **说明**：`navigationBarTitleText` 会在页面 `onLoad` 时根据 API 返回的 `title` 字段动态覆盖（`uni.setNavigationBarTitle`）。

### 2.2 现有页面修改

| 页面 | 变更说明 |
|------|----------|
| `pages/index/index` | 首页改造为 CMS 动态渲染模式，保留原有硬编码内容作为降级兜底 |

### 2.3 新增文件清单

```
uni-app/src/
├── components/cms/
│   ├── CmsRenderer.vue          # CMS 渲染引擎主组件
│   ├── CmsBanner.vue            # Banner 轮播图
│   ├── CmsImage.vue             # 图片区块
│   ├── CmsImageText.vue         # 图文卡片
│   ├── CmsNotice.vue            # 公告栏
│   ├── CmsNav.vue               # 快捷导航
│   ├── CmsProductList.vue       # 商品列表
│   ├── CmsCoupon.vue            # 优惠券区块
│   ├── CmsRichText.vue          # 富文本
│   ├── CmsSpacer.vue            # 间距
│   ├── CmsDivider.vue           # 分割线
│   └── CmsVideo.vue             # 视频区块
├── pages/cms-page/
│   └── index.vue                # CMS 通用页面容器
├── utils/
│   └── cms-link.ts              # 超链接跳转工具
├── api/
│   └── cms.ts                   # CMS 相关 API 接口封装
└── types/
    └── cms.ts                   # CMS 类型定义
```

---

## 3. CMS 渲染引擎设计

### 3.1 类型定义 — `types/cms.ts`

首先定义完整的 TypeScript 类型，所有组件的 props 均严格类型化：

```typescript
/** CMS 链接配置 */
export interface CmsLink {
  /** 链接类型 */
  type: 'page' | 'product' | 'category' | 'activity' | 'h5' | 'miniprogram' | 'none'
  /** 跳转目标：页面路径 / 商品ID / 分类ID / page_code / URL / appId */
  target: string
  /** 链接标题（用于展示和调试） */
  title?: string
  /** 小程序跳转附加路径（仅 type=miniprogram） */
  path?: string
}

/** 组件通用样式 */
export interface CmsComponentStyle {
  margin_top?: number
  margin_bottom?: number
  padding_top?: number
  padding_bottom?: number
  padding_left?: number
  padding_right?: number
  background?: string
  border_radius?: number
}

/** Banner 轮播图 props */
export interface CmsBannerProps {
  images: Array<{
    url: string
    link: CmsLink
  }>
  /** 轮播间隔（秒），默认 5 */
  interval?: number
  /** 指示器样式：dot / number / none */
  indicator_style?: 'dot' | 'number' | 'none'
}

/** 图片区块 props */
export interface CmsImageProps {
  images: Array<{
    url: string
    link: CmsLink
  }>
  /** 布局模式 */
  layout: 'single' | 'two-column' | 'three-column' | 'four-grid'
}

/** 图文卡片 props */
export interface CmsImageTextProps {
  image: string
  title: string
  subtitle?: string
  description?: string
  link: CmsLink
  /** 布局方向 */
  layout?: 'horizontal' | 'vertical'
}

/** 公告栏 props */
export interface CmsNoticeProps {
  notices: Array<{
    text: string
    link?: CmsLink
  }>
  /** 滚动速度（px/s），默认 50 */
  speed?: number
  /** 背景色，默认使用主题 accent-bg */
  background_color?: string
  /** 左侧图标 URL 或 emoji */
  icon?: string
}

/** 快捷导航 props */
export interface CmsNavProps {
  columns: 3 | 4 | 5
  items: Array<{
    icon: string
    name: string
    link: CmsLink
  }>
}

/** 商品列表 props */
export interface CmsProductListProps {
  /** 商品来源 */
  source: 'manual' | 'category' | 'tag'
  /** 手动选择的商品 ID 列表 */
  product_ids?: number[]
  /** 按分类筛选（category_key） */
  category_key?: string
  /** 按标签筛选 */
  tag?: string
  /** 展示数量，默认 6 */
  count?: number
  /** 布局模式 */
  layout?: 'list' | 'grid'
}

/** 优惠券区块 props */
export interface CmsCouponProps {
  coupon_ids: number[]
  /** 布局样式 */
  layout?: 'horizontal' | 'vertical'
}

/** 富文本 props */
export interface CmsRichTextProps {
  /** HTML 内容（已经过后端 nh3 sanitize） */
  content: string
}

/** 间距 props */
export interface CmsSpacerProps {
  /** 高度（rpx） */
  height: number
}

/** 分割线 props */
export interface CmsDividerProps {
  /** 线型 */
  line_style: 'solid' | 'dashed'
  /** 线颜色 */
  color?: string
  /** 左右边距（rpx） */
  margin_horizontal?: number
}

/** 视频区块 props */
export interface CmsVideoProps {
  /** 视频 URL */
  url: string
  /** 封面图 */
  poster?: string
  /** 自动播放 */
  autoplay?: boolean
  /** 循环播放 */
  loop?: boolean
}

/** 组件类型枚举 */
export type CmsComponentType =
  | 'banner'
  | 'image'
  | 'image_text'
  | 'notice'
  | 'nav'
  | 'product_list'
  | 'coupon'
  | 'rich_text'
  | 'spacer'
  | 'divider'
  | 'video'

/** 组件类型 → Props 类型映射 */
export interface CmsPropsMap {
  banner: CmsBannerProps
  image: CmsImageProps
  image_text: CmsImageTextProps
  notice: CmsNoticeProps
  nav: CmsNavProps
  product_list: CmsProductListProps
  coupon: CmsCouponProps
  rich_text: CmsRichTextProps
  spacer: CmsSpacerProps
  divider: CmsDividerProps
  video: CmsVideoProps
}

/** 单个组件配置 */
export interface CmsComponentConfig<T extends CmsComponentType = CmsComponentType> {
  id: string
  type: T
  props: CmsPropsMap[T]
  style?: CmsComponentStyle
}

/** 页面设置 */
export interface CmsPageSettings {
  background_color?: string
  title_bar_color?: string
  title_bar_text_color?: string
}

/** 页面配置 JSON（对应后端 draft_config / version config） */
export interface CmsPageConfig {
  version: number
  page_settings: CmsPageSettings
  components: CmsComponentConfig[]
}

/** C端页面配置 API 响应 data */
export interface CmsPageResponse {
  page_code: string
  title: string
  /** 版本号，用于与本地缓存对比 */
  version: number
  config: CmsPageConfig
  updated_at: string
}

/** 本地缓存结构 */
export interface CmsPageCache {
  version: number
  config: CmsPageConfig
  title: string
  cached_at: number
}
```

### 3.2 CmsRenderer 组件架构

**文件路径**：`components/cms/CmsRenderer.vue`

CmsRenderer 是渲染引擎的核心入口组件，负责：
1. 接收 `CmsPageConfig` 配置 JSON
2. 应用 `page_settings`（背景色等）
3. 遍历 `components` 数组，通过组件注册表映射到对应 Vue 组件
4. 为每个组件传递 `props` 和 `style`
5. 遇到未知组件类型时静默跳过（`console.warn` 记录）
6. 单个组件渲染异常时不影响其他组件

```vue
<template>
  <view
    class="cms-renderer"
    :style="{ backgroundColor: config?.page_settings?.background_color || '' }"
  >
    <template v-for="comp in validComponents" :key="comp.id">
      <view
        class="cms-component-wrapper"
        :style="buildWrapperStyle(comp.style)"
      >
        <!-- 错误边界：单个组件渲染异常时不影响其他组件 -->
        <component
          :is="getComponent(comp.type)"
          :data="comp.props"
          :component-style="comp.style"
          @link-tap="onLinkTap"
        />
      </view>
    </template>
  </view>
</template>

<script setup lang="ts">
import { computed, onErrorCaptured } from 'vue'
import type { CmsPageConfig, CmsComponentConfig, CmsComponentStyle, CmsLink } from '@/types/cms'
import { handleCmsLink } from '@/utils/cms-link'
import { componentRegistry } from './registry'

interface Props {
  config: CmsPageConfig | null
}

const props = defineProps<Props>()

/**
 * 组件级错误边界（M01）
 * 捕获子组件渲染异常，防止单个组件错误导致整个 CmsRenderer 白屏
 * 返回 false 阻止错误继续向上冒泡
 */
onErrorCaptured((err, instance, info) => {
  console.warn(`[CmsRenderer] 组件渲染异常:`, {
    error: err,
    component: instance?.$options?.name || '未知组件',
    info,
  })
  // 返回 false 阻止错误向上传播，保证其他组件正常渲染
  return false
})

/** 过滤掉未知组件类型，保证只渲染已注册的组件 */
const validComponents = computed(() => {
  if (!props.config?.components) return []
  return props.config.components.filter((comp) => {
    if (componentRegistry[comp.type]) return true
    console.warn(`[CmsRenderer] 未知组件类型: ${comp.type}，已跳过`)
    return false
  })
})

/** 根据 type 获取组件定义 */
function getComponent(type: string) {
  return componentRegistry[type] || null
}

/** 构建组件外层容器样式 */
function buildWrapperStyle(style?: CmsComponentStyle): Record<string, string> {
  if (!style) return {}
  const result: Record<string, string> = {}
  if (style.margin_top != null) result.marginTop = `${style.margin_top}rpx`
  if (style.margin_bottom != null) result.marginBottom = `${style.margin_bottom}rpx`
  if (style.padding_top != null) result.paddingTop = `${style.padding_top}rpx`
  if (style.padding_bottom != null) result.paddingBottom = `${style.padding_bottom}rpx`
  if (style.padding_left != null) result.paddingLeft = `${style.padding_left}rpx`
  if (style.padding_right != null) result.paddingRight = `${style.padding_right}rpx`
  if (style.background) result.background = style.background
  if (style.border_radius != null) result.borderRadius = `${style.border_radius}rpx`
  return result
}

/** 统一处理组件内的链接点击 */
function onLinkTap(link: CmsLink) {
  handleCmsLink(link)
}
</script>

<style lang="scss" scoped>
.cms-renderer {
  min-height: 100vh;
  width: 100%;
}

.cms-component-wrapper {
  width: 100%;
  overflow: hidden;
}
</style>
```

### 3.3 组件注册表 — `components/cms/registry.ts`

使用静态映射表将 `type` 字符串映射到 Vue 组件。**不使用动态 `import()`**，因为小程序不支持动态导入，所有组件在构建时静态注册。

```typescript
/**
 * CMS 组件注册表
 * type → Vue Component 的映射
 * 新增组件类型时在此处注册即可
 */
import type { Component } from 'vue'
import CmsBanner from './CmsBanner.vue'
import CmsImage from './CmsImage.vue'
import CmsImageText from './CmsImageText.vue'
import CmsNotice from './CmsNotice.vue'
import CmsNav from './CmsNav.vue'
import CmsProductList from './CmsProductList.vue'
import CmsCoupon from './CmsCoupon.vue'
import CmsRichText from './CmsRichText.vue'
import CmsSpacer from './CmsSpacer.vue'
import CmsDivider from './CmsDivider.vue'
import CmsVideo from './CmsVideo.vue'

export const componentRegistry: Record<string, Component> = {
  banner: CmsBanner,
  image: CmsImage,
  image_text: CmsImageText,
  notice: CmsNotice,
  nav: CmsNav,
  product_list: CmsProductList,
  coupon: CmsCoupon,
  rich_text: CmsRichText,
  spacer: CmsSpacer,
  divider: CmsDivider,
  video: CmsVideo,
}

/** 已注册的组件类型列表（用于校验） */
export const registeredTypes = Object.keys(componentRegistry)
```

### 3.4 各 CMS 组件详细设计

所有组件遵循统一接口约定：
- 接收 `data` prop（组件的配置数据）和 `componentStyle` prop（样式覆盖）
- 需要跳转时 emit `link-tap` 事件，由 CmsRenderer 统一处理
- 组件内部做好数据防御（data 可能不完整或类型异常，用可选链 + 默认值兜底）

---

#### 3.4.1 Banner 轮播图 — `CmsBanner.vue`

**文件路径**：`components/cms/CmsBanner.vue`

**Props 定义**：
```typescript
interface Props {
  data: CmsBannerProps  // { images, interval?, indicator_style? }
  componentStyle?: CmsComponentStyle
}
```

**配置 JSON 示例**：
```json
{
  "type": "banner",
  "props": {
    "images": [
      { "url": "https://cdn.example.com/banner1.jpg", "link": { "type": "product", "target": "123", "title": "星空帐篷" } },
      { "url": "https://cdn.example.com/banner2.jpg", "link": { "type": "page", "target": "/pages/category/index", "title": "分类页" } }
    ],
    "interval": 5,
    "indicator_style": "dot"
  }
}
```

**渲染逻辑**：
1. 使用 `<swiper>` 组件，设置 `autoplay`、`circular`、`interval`（data.interval × 1000ms）
2. 遍历 `data.images` 渲染 `<swiper-item>`，每项包含 `<image>` 标签
3. 图片 URL 通过 `resolveImageUrl()` 处理（兼容相对路径和 http→https 升级）
4. 点击图片时 emit `link-tap(image.link)`
5. 根据 `indicator_style` 渲染自定义指示器（dot 圆点、number 数字、none 隐藏）
6. 图片加载失败时显示占位背景色

**样式设计**：
```scss
.cms-banner {
  width: 100%;
  overflow: hidden;

  &__swiper {
    height: 340rpx; // 默认高度，可由 style 覆盖
  }

  &__image {
    width: 100%;
    height: 100%;
    border-radius: var(--radius-xl); // 32rpx 圆角
  }

  // 指示器 — 暖铜金色活跃态
  &__indicator {
    display: flex;
    justify-content: center;
    gap: 10rpx;
    margin-top: 16rpx;
  }

  &__dot {
    width: 8rpx;
    height: 8rpx;
    border-radius: 50%;
    background-color: var(--color-text-placeholder);
    opacity: 0.3;
    transition: all 0.4s var(--ease-out-expo);

    &--active {
      width: 28rpx;
      border-radius: 4rpx;
      background: linear-gradient(90deg, var(--color-accent), var(--color-primary-light));
      opacity: 1;
    }
  }
}
```

---

#### 3.4.2 图片区块 — `CmsImage.vue`

**文件路径**：`components/cms/CmsImage.vue`

**Props 定义**：
```typescript
interface Props {
  data: CmsImageProps  // { images, layout }
  componentStyle?: CmsComponentStyle
}
```

**配置 JSON 示例**：
```json
{
  "type": "image",
  "props": {
    "images": [
      { "url": "https://cdn.example.com/img1.jpg", "link": { "type": "h5", "target": "https://example.com" } },
      { "url": "https://cdn.example.com/img2.jpg", "link": { "type": "none", "target": "" } }
    ],
    "layout": "two-column"
  }
}
```

**渲染逻辑**：
1. 根据 `layout` 值决定 CSS Grid 布局：
   - `single`：单图全宽，`grid-template-columns: 1fr`
   - `two-column`：两列等宽，`grid-template-columns: repeat(2, 1fr)`
   - `three-column`：三列等宽，`grid-template-columns: repeat(3, 1fr)`
   - `four-grid`：四宫格 2×2，`grid-template-columns: repeat(2, 1fr)`
2. 图片使用 `mode="aspectFill"` 填充，长按可预览大图（`uni.previewImage`）
3. 点击图片时如有 link 且 `type !== 'none'`，emit `link-tap`

**样式设计**：
```scss
.cms-image {
  padding: 0 var(--spacing-lg); // 两侧边距 28rpx

  &__grid {
    display: grid;
    gap: 12rpx;
    // grid-template-columns 根据 layout 动态设置
  }

  &__item {
    border-radius: var(--radius-lg); // 24rpx
    overflow: hidden;
    position: relative;
  }

  &__img {
    width: 100%;
    // 单图高度 auto，多图正方形 aspect-ratio: 1
    border-radius: var(--radius-lg);
  }
}
```

---

#### 3.4.3 图文卡片 — `CmsImageText.vue`

**文件路径**：`components/cms/CmsImageText.vue`

**Props 定义**：
```typescript
interface Props {
  data: CmsImageTextProps  // { image, title, subtitle?, description?, link, layout? }
  componentStyle?: CmsComponentStyle
}
```

**配置 JSON 示例**：
```json
{
  "type": "image_text",
  "props": {
    "image": "https://cdn.example.com/camp.jpg",
    "title": "春日露营季",
    "subtitle": "3月限时特惠",
    "description": "享受春日阳光，感受大自然的美好...",
    "link": { "type": "activity", "target": "spring_2026", "title": "春季活动" },
    "layout": "horizontal"
  }
}
```

**渲染逻辑**：
1. `vertical` 布局：上图下文，图片全宽展示
2. `horizontal` 布局（默认）：左图右文，图片占 40% 宽度
3. 标题使用 `--font-size-lg`，副标题使用 `--font-size-sm` 暖铜金色，描述使用 `--font-size-base` 次要文字色
4. 整体卡片可点击，emit `link-tap`

**样式设计**：
```scss
.cms-image-text {
  margin: 0 var(--spacing-lg);
  background: var(--color-bg-card);
  border-radius: var(--radius-xl);
  overflow: hidden;
  box-shadow: var(--shadow-sm);
  border: 1rpx solid rgba(42, 37, 32, 0.04);

  // 水平布局
  &--horizontal {
    display: flex;
    align-items: stretch;
  }

  &__image {
    width: 100%;
    height: 320rpx;
    // 水平布局下 width: 280rpx; flex-shrink: 0
  }

  &__content {
    padding: var(--spacing-lg);
    flex: 1;
  }

  &__title {
    font-size: var(--font-size-lg);
    font-weight: 700;
    color: var(--color-text);
    letter-spacing: 1rpx;
    line-height: 1.4;
  }

  &__subtitle {
    font-size: var(--font-size-sm);
    color: var(--color-accent); // 暖铜金
    margin-top: 8rpx;
    letter-spacing: 1rpx;
  }

  &__description {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    margin-top: 12rpx;
    line-height: 1.6;
    // 最多3行省略
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
}
```

---

#### 3.4.4 公告栏 — `CmsNotice.vue`

**文件路径**：`components/cms/CmsNotice.vue`

**Props 定义**：
```typescript
interface Props {
  data: CmsNoticeProps  // { notices, speed?, background_color?, icon? }
  componentStyle?: CmsComponentStyle
}
```

**配置 JSON 示例**：
```json
{
  "type": "notice",
  "props": {
    "notices": [
      { "text": "🎪 五一特别活动火热报名中", "link": { "type": "activity", "target": "may_day", "title": "五一活动" } },
      { "text": "⛺ 新营位上线，限时8折" }
    ],
    "speed": 50,
    "icon": "📢"
  }
}
```

**渲染逻辑**：
1. 使用横向滚动动画（CSS `@keyframes scroll`）循环展示公告文字
2. 多条公告时轮流展示（`setInterval` 切换，间隔 4 秒）
3. 点击公告时若有 link，emit `link-tap`
4. 左侧展示 icon（默认 📢），右侧展示公告文字

**样式设计**：
```scss
.cms-notice {
  margin: 0 var(--spacing-lg);
  padding: 16rpx 24rpx;
  background: var(--color-accent-bg); // rgba(200, 168, 114, 0.08)
  border-radius: var(--radius-lg);
  display: flex;
  align-items: center;
  gap: 12rpx;
  overflow: hidden;

  &__icon {
    flex-shrink: 0;
    font-size: 28rpx;
  }

  &__text {
    flex: 1;
    font-size: var(--font-size-sm);
    color: var(--color-text);
    white-space: nowrap;
    overflow: hidden;
  }

  &__scroll {
    animation: cms-notice-scroll linear infinite;
  }
}
```

---

#### 3.4.5 快捷导航 — `CmsNav.vue`

**文件路径**：`components/cms/CmsNav.vue`

**Props 定义**：
```typescript
interface Props {
  data: CmsNavProps  // { columns, items }
  componentStyle?: CmsComponentStyle
}
```

**配置 JSON 示例**：
```json
{
  "type": "nav",
  "props": {
    "columns": 4,
    "items": [
      { "icon": "https://cdn.example.com/icon_camp.png", "name": "营位预定", "link": { "type": "page", "target": "/pages/category/index" } },
      { "icon": "🏕️", "name": "日常露营", "link": { "type": "category", "target": "daily_camping" } }
    ]
  }
}
```

**渲染逻辑**：
1. 使用 CSS Grid 布局，`grid-template-columns: repeat({columns}, 1fr)`
2. 每个导航项：上方图标（图片 URL 则用 `<image>`，emoji/text 则用 `<text>`） + 下方名称
3. 点击导航项 emit `link-tap(item.link)`
4. 按压反馈：`transform: scale(0.92)`

**样式设计**：
```scss
.cms-nav {
  padding: var(--spacing-lg) var(--spacing-lg) var(--spacing-md);

  &__grid {
    display: grid;
    gap: 24rpx 16rpx;
    // grid-template-columns 由 columns prop 决定
  }

  &__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 4rpx 0;
    transition: var(--transition-base);

    &:active {
      transform: scale(0.92);
    }
  }

  &__icon {
    width: 88rpx;
    height: 88rpx;
    display: flex;
    justify-content: center;
    align-items: center;
    border-radius: var(--radius-xl);
    background: var(--color-bg-card);
    box-shadow: var(--shadow-sm);
    margin-bottom: 12rpx;

    image {
      width: 48rpx;
      height: 48rpx;
    }
  }

  &__name {
    font-size: var(--font-size-xs);
    color: var(--color-text-secondary);
    font-weight: 500;
    letter-spacing: 1rpx;
  }
}
```

---

#### 3.4.6 商品列表 — `CmsProductList.vue`

**文件路径**：`components/cms/CmsProductList.vue`

**Props 定义**：
```typescript
interface Props {
  data: CmsProductListProps  // { source, product_ids?, category_key?, tag?, count?, layout? }
  componentStyle?: CmsComponentStyle
}
```

**配置 JSON 示例**：
```json
{
  "type": "product_list",
  "props": {
    "source": "category",
    "category_key": "daily_camping",
    "count": 6,
    "layout": "grid"
  }
}
```

**渲染逻辑**：
1. **组件挂载时主动请求商品数据**（这是唯一需要发网络请求的 CMS 组件）
2. 根据 `source` 构建不同的 API 请求参数：
   - `manual`：`GET /api/v1/products?ids={product_ids.join(',')}`
   - `category`：`GET /api/v1/products?category={category_key}&page_size={count}`
   - `tag`：`GET /api/v1/products?tag={tag}&page_size={count}`
3. `layout=grid` 双列瀑布流（复用现有 `ProductCard` 组件的 `mode="grid"`）
4. `layout=list` 单列列表
5. 加载中展示骨架屏，加载失败静默隐藏
6. 点击商品自动跳转商品详情页（无需配置 link）

**样式设计**：
```scss
.cms-product-list {
  padding: 0 var(--spacing-lg);

  // grid 模式 — 复用首页双列瀑布流样式
  &__grid {
    display: flex;
    gap: 16rpx;
  }

  &__col {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 16rpx;
  }

  // list 模式 — 单列卡片
  &__list {
    display: flex;
    flex-direction: column;
    gap: 16rpx;
  }
}
```

---

#### 3.4.7 优惠券区块 — `CmsCoupon.vue`

**文件路径**：`components/cms/CmsCoupon.vue`

**Props 定义**：
```typescript
interface Props {
  data: CmsCouponProps  // { coupon_ids, layout? }
  componentStyle?: CmsComponentStyle
}
```

**配置 JSON 示例**：
```json
{
  "type": "coupon",
  "props": {
    "coupon_ids": [1, 2, 3],
    "layout": "horizontal"
  }
}
```

**渲染逻辑**：
1. 组件挂载时请求优惠券详情列表 API
2. `horizontal`：横向滚动展示（`scroll-view` + `scroll-x`）
3. `vertical`：纵向堆叠
4. 每张优惠券卡片展示：券面额、使用条件、有效期、领取按钮
5. 点击「领取」调用领券 API，领取成功后按钮变为「已领取」
6. 加载失败或无数据时静默隐藏
7. **未登录处理**：点击「领取」时先检查登录状态（从 Pinia userStore 读取），未登录则调用 `uni.navigateTo({ url: '/pages/login/index' })` 跳转登录页，登录成功后返回时自动重试领券
8. **领券失败状态 UI**：
   - 已领取过 → 按钮灰色显示「已领取」，不可点击
   - 已抢完（库存为 0）→ 按钮灰色显示「已抢光」，不可点击
   - 不在有效期（未开始）→ 按钮灰色显示「未开始」，不可点击
   - 已过期 → 按钮灰色显示「已过期」，不可点击
   - 其他领取失败 → `uni.showToast({ title: '领取失败，请稍后重试', icon: 'none' })`

**样式设计**：
```scss
.cms-coupon {
  padding: 0 var(--spacing-lg);

  &__scroll {
    white-space: nowrap;
  }

  &__card {
    display: inline-flex;
    width: 320rpx;
    background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
    border-radius: var(--radius-lg);
    padding: 20rpx 24rpx;
    margin-right: 16rpx;
    color: var(--color-text-white);
    position: relative;
    overflow: hidden;

    // 优惠券锯齿边缘
    &::before, &::after {
      content: '';
      position: absolute;
      width: 24rpx;
      height: 24rpx;
      background: var(--color-bg);
      border-radius: 50%;
      top: 50%;
      transform: translateY(-50%);
    }
    &::before { left: -12rpx; }
    &::after { right: -12rpx; }
  }

  &__amount {
    font-size: var(--font-size-hero);
    font-weight: 800;
    color: var(--color-accent); // 暖铜金
  }

  &__btn {
    font-size: var(--font-size-xs);
    padding: 8rpx 20rpx;
    border: 1rpx solid rgba(255, 255, 255, 0.6);
    border-radius: var(--radius-round);
    color: var(--color-text-white);

    // 不可领取状态（已领取/已抢光/未开始/已过期）
    &--disabled {
      opacity: 0.5;
      border-color: rgba(255, 255, 255, 0.3);
      pointer-events: none;
    }
  }
}
```

---

#### 3.4.8 富文本 — `CmsRichText.vue`

**文件路径**：`components/cms/CmsRichText.vue`

**Props 定义**：
```typescript
interface Props {
  data: CmsRichTextProps  // { content }
  componentStyle?: CmsComponentStyle
}
```

**配置 JSON 示例**：
```json
{
  "type": "rich_text",
  "props": {
    "content": "<h2>品牌故事</h2><p>一月一露，源于对自然的热爱...</p>"
  }
}
```

**渲染逻辑**：
1. **使用 uni-app 原生 `<rich-text>` 组件**（非 `v-html`），原生组件自动过滤危险标签（`<script>`、`<iframe>` 等），这是小程序端的 XSS 防护措施
2. 内容为空时不渲染
3. 内容中的图片自动设置 `max-width: 100%` 防止溢出
4. 链接点击处理：rich-text 组件不支持内部链接点击事件（小程序限制），如需支持可改用 `mp-html` 第三方组件

**样式设计**：
```scss
.cms-rich-text {
  padding: 0 var(--spacing-lg);

  &__content {
    font-size: var(--font-size-base);
    color: var(--color-text);
    line-height: 1.8;
    letter-spacing: 0.5rpx;

    // 全局图片样式（通过外层 class 控制）
    image {
      max-width: 100% !important;
      border-radius: var(--radius-md);
    }
  }
}
```

---

#### 3.4.9 间距 — `CmsSpacer.vue`

**文件路径**：`components/cms/CmsSpacer.vue`

**Props 定义**：
```typescript
interface Props {
  data: CmsSpacerProps  // { height }
  componentStyle?: CmsComponentStyle
}
```

**渲染逻辑**：
- 渲染一个空 `<view>`，高度为 `data.height + 'rpx'`
- 无交互，无样式装饰

```vue
<template>
  <view class="cms-spacer" :style="{ height: (data.height || 20) + 'rpx' }" />
</template>
```

---

#### 3.4.10 分割线 — `CmsDivider.vue`

**文件路径**：`components/cms/CmsDivider.vue`

**Props 定义**：
```typescript
interface Props {
  data: CmsDividerProps  // { line_style, color?, margin_horizontal? }
  componentStyle?: CmsComponentStyle
}
```

**渲染逻辑**：
- 渲染一条水平线，`border-style` 对应 `line_style`（solid/dashed）
- 颜色默认为 `rgba(42, 37, 32, 0.08)`
- 左右边距由 `margin_horizontal` 控制

```vue
<template>
  <view
    class="cms-divider"
    :style="{
      borderTopStyle: data.line_style || 'solid',
      borderTopColor: data.color || 'rgba(42, 37, 32, 0.08)',
      marginLeft: (data.margin_horizontal || 28) + 'rpx',
      marginRight: (data.margin_horizontal || 28) + 'rpx',
    }"
  />
</template>

<style lang="scss" scoped>
.cms-divider {
  border-top-width: 1rpx;
}
</style>
```

---

#### 3.4.11 视频区块 — `CmsVideo.vue`

**文件路径**：`components/cms/CmsVideo.vue`

**Props 定义**：
```typescript
interface Props {
  data: CmsVideoProps  // { url, poster?, autoplay?, loop? }
  componentStyle?: CmsComponentStyle
}
```

**配置 JSON 示例**：
```json
{
  "type": "video",
  "props": {
    "url": "https://cdn.example.com/brand.mp4",
    "poster": "https://cdn.example.com/brand_poster.jpg",
    "autoplay": false,
    "loop": true
  }
}
```

**渲染逻辑**：
1. 使用 uni-app 原生 `<video>` 组件
2. 设置 `poster`（封面图）、`autoplay`、`loop`、`show-fullscreen-btn`
3. `autoplay` 默认 `false`（避免流量消耗和自动播放限制）
4. 视频加载失败时展示封面图 + 错误提示
5. 支持全屏播放

**样式设计**：
```scss
.cms-video {
  padding: 0 var(--spacing-lg);

  &__player {
    width: 100%;
    border-radius: var(--radius-xl);
    overflow: hidden;
  }

  // 播放失败兜底
  &__error {
    width: 100%;
    height: 400rpx;
    background: var(--color-bg-light);
    border-radius: var(--radius-xl);
    display: flex;
    justify-content: center;
    align-items: center;
    color: var(--color-text-placeholder);
    font-size: var(--font-size-sm);
  }
}
```

---

## 4. 超链接跳转工具 — `utils/cms-link.ts`

### 4.1 完整实现设计

```typescript
/**
 * CMS 超链接跳转工具
 * 统一处理 7 种链接类型的跳转逻辑
 */
import type { CmsLink } from '@/types/cms'

/** tabBar 页面路径列表（switchTab 判断） */
const TAB_BAR_PAGES = [
  '/pages/index/index',
  '/pages/category/index',
  '/pages/cart/index',
  '/pages/order/index',
  '/pages/mine/index',
]

/**
 * 判断路径是否为 tabBar 页面
 */
function isTabBarPage(path: string): boolean {
  const cleanPath = path.split('?')[0]
  return TAB_BAR_PAGES.includes(cleanPath)
}

/**
 * 处理 CMS 链接跳转
 * @param link CMS 链接配置对象
 */
export function handleCmsLink(link: CmsLink): void {
  if (!link || link.type === 'none' || !link.target) {
    return
  }

  switch (link.type) {
    case 'page':
      handlePageLink(link.target)
      break

    case 'product':
      handleProductLink(link.target)
      break

    case 'category':
      handleCategoryLink(link.target)
      break

    case 'activity':
      handleActivityLink(link.target)
      break

    case 'h5':
      handleH5Link(link.target)
      break

    case 'miniprogram':
      handleMiniprogramLink(link.target, link.path)
      break

    default:
      console.warn(`[cms-link] 未知链接类型: ${link.type}`)
  }
}

/**
 * page — 小程序内部页面跳转
 * target 为完整页面路径，如 "/pages/product-detail/index?id=123"
 *
 * 判断逻辑：
 * - 如果是 tabBar 页面 → switchTab（switchTab 不支持带参数，自动去掉 query）
 * - 否则 → navigateTo
 * - navigateTo 失败（页面栈满10层）→ 降级 redirectTo
 */
function handlePageLink(target: string): void {
  if (isTabBarPage(target)) {
    uni.switchTab({ url: target.split('?')[0] })
  } else {
    uni.navigateTo({
      url: target,
      fail: () => {
        // 页面栈满时降级为 redirectTo
        uni.redirectTo({ url: target })
      },
    })
  }
}

/**
 * product — 商品详情页
 * target 为商品 ID，自动拼接详情页路径
 */
function handleProductLink(target: string): void {
  const url = `/pages/product-detail/index?id=${target}`
  uni.navigateTo({
    url,
    fail: () => {
      uni.redirectTo({ url })
    },
  })
}

/**
 * category — 商品分类页
 * target 为分类 key（如 "daily_camping"），跳转到分类页并传递筛选参数
 *
 * 注意：分类页 `/pages/category/index` 是 tabBar 页面，
 *       switchTab 不支持传参，需通过全局事件或 Pinia store 传递 category_key
 */
function handleCategoryLink(target: string): void {
  // 通过全局事件总线传递分类参数
  uni.$emit('cms:category-switch', { category_key: target })
  uni.switchTab({ url: '/pages/category/index' })
}

/**
 * activity — CMS 装修活动页
 * target 为 page_code，跳转到 CMS 通用页面容器
 */
function handleActivityLink(target: string): void {
  const url = `/pages/cms-page/index?page_code=${target}`
  uni.navigateTo({
    url,
    fail: () => {
      uni.redirectTo({ url })
    },
  })
}

/**
 * h5 — 外部 H5 链接
 * target 为完整 URL，通过 web-view 打开
 *
 * 注意：
 * - 微信小程序 web-view 要求域名在小程序后台配置业务域名白名单
 * - 需要在 pages-sub 中注册 web-view 页面
 */
function handleH5Link(target: string): void {
  // 校验 URL 格式
  if (!target.startsWith('http://') && !target.startsWith('https://')) {
    console.warn(`[cms-link] 无效的 H5 链接: ${target}`)
    uni.showToast({ title: '链接无效', icon: 'none' })
    return
  }

  // M02: 复用 game-webview 页面，通过 title 参数动态设置导航栏标题
  // 需修改 game-webview 页面支持 title query 参数（onLoad 时 uni.setNavigationBarTitle）
  // 后续如需更语义化可新增 pages-sub/common/webview/index 通用 webview 页面
  const url = `/pages-sub/product/game-webview/index?url=${encodeURIComponent(target)}&title=${encodeURIComponent('详情')}`
  uni.navigateTo({
    url,
    fail: () => {
      uni.redirectTo({ url })
    },
  })
}

/**
 * miniprogram — 其他小程序跳转
 * target 为目标小程序 appId，path 为小程序内路径
 *
 * 注意：微信小程序需在 app.json 或后台配置 navigateToMiniProgramAppIdList
 */
function handleMiniprogramLink(appId: string, path?: string): void {
  // #ifdef MP-WEIXIN
  wx.navigateToMiniProgram({
    appId,
    path: path || '',
    fail: () => {
      uni.showToast({ title: '跳转失败，请稍后重试', icon: 'none' })
    },
  })
  // #endif

  // #ifdef H5
  uni.showToast({ title: '请在微信小程序中打开', icon: 'none' })
  // #endif
}
```

### 4.2 跳转方式判断逻辑总结

| 链接类型 | 跳转 API | 判断依据 | 降级策略 |
|----------|---------|----------|----------|
| `page` | `navigateTo` / `switchTab` | 路径是否在 tabBar 列表中 | `redirectTo` |
| `product` | `navigateTo` | 固定跳商品详情页 | `redirectTo` |
| `category` | `switchTab` | 分类页是 tabBar 页面 | 事件总线传参 |
| `activity` | `navigateTo` | 跳 CMS 通用容器 | `redirectTo` |
| `h5` | `navigateTo` (web-view) | 外部链接 | URL 校验 |
| `miniprogram` | `navigateToMiniProgram` | 条件编译仅微信 | H5 端提示 |
| `none` | 不跳转 | — | — |

---

## 5. 首页改造方案

### 5.1 改造思路

将 `pages/index/index.vue` 从完全硬编码改为「CMS 优先 + 硬编码兜底」的混合模式：

```
┌───────────────────────────────────────┐
│           首页加载流程                  │
│                                       │
│  onLoad ──→ 读本地缓存                │
│              │                        │
│     有缓存且未过期？                    │
│       ├── 是 → 立即渲染缓存版本         │
│       │        后台静默请求API刷新       │
│       └── 否 → 展示骨架屏              │
│                请求API获取配置          │
│                  │                    │
│        API成功？                       │
│       ├── 是 → 对比版本号              │
│       │   ├── 不同 → 更新渲染+更新缓存  │
│       │   └── 相同 → 保持当前           │
│       └── 否 → 有缓存？               │
│           ├── 是 → 渲染过期缓存         │
│           └── 否 → 渲染内置默认首页     │
└───────────────────────────────────────┘
```

### 5.2 API 请求

```typescript
// api/cms.ts
import { get } from '@/utils/request'
import type { CmsPageResponse } from '@/types/cms'

/**
 * 获取已发布的页面配置（C端公开接口）
 * 无需登录认证（needAuth: false）
 */
export function getCmsPage(pageCode: string, previewToken?: string) {
  return get<CmsPageResponse>(
    `/cms/pages/${pageCode}`,
    previewToken ? { preview_token: previewToken } : undefined,
    { needAuth: false, showError: false }
  )
}

/**
 * 检查页面版本是否有更新
 */
export function checkCmsPageVersion(pageCode: string, currentVersion: number) {
  return get<{ has_update: boolean; version: number }>(
    `/cms/pages/${pageCode}/check`,
    { version: currentVersion },
    { needAuth: false, showError: false }
  )
}
```

### 5.3 本地缓存策略

利用已有的 `utils/storage.ts` 封装的 `setStorage` / `getStorage`（支持过期时间），设计如下缓存机制：

```typescript
// 缓存 key 常量
const CMS_PAGE_CACHE_PREFIX = 'cms_page_config:'
const CMS_CACHE_EXPIRE_SECONDS = 600 // 10分钟

/**
 * 读取页面缓存
 */
export function getCmsPageCache(pageCode: string): CmsPageCache | null {
  return getStorage<CmsPageCache>(`${CMS_PAGE_CACHE_PREFIX}${pageCode}`)
}

/**
 * 写入页面缓存
 */
export function setCmsPageCache(pageCode: string, data: CmsPageResponse): void {
  const cache: CmsPageCache = {
    version: data.version,
    config: data.config,
    title: data.title,
    cached_at: Date.now(),
  }
  setStorage(`${CMS_PAGE_CACHE_PREFIX}${pageCode}`, cache, CMS_CACHE_EXPIRE_SECONDS)
}

/**
 * 清除指定页面缓存
 */
export function removeCmsPageCache(pageCode: string): void {
  removeStorage(`${CMS_PAGE_CACHE_PREFIX}${pageCode}`)
}
```

> 在 `STORAGE_KEYS` 中追加 `CMS_PAGE_PREFIX: 'cms_page_config:'` 常量。

### 5.4 三级降级方案

| 优先级 | 数据来源 | 触发条件 | 用户体验 |
|--------|---------|----------|----------|
| L1 | 服务端 API | 正常情况 | CMS 动态渲染最新内容 |
| L2 | 本地缓存 | API 请求失败 + 本地有缓存（含过期缓存） | 渲染上次缓存的内容（可能略旧） |
| L3 | 内置默认首页 | API 失败 + 无缓存 | 渲染原有硬编码首页（保证基本可用） |

**实现要点**：
- L1/L2 使用 `<CmsRenderer :config="pageConfig" />` 渲染
- L3 使用原有首页的硬编码 template（封装为 `DefaultHomePage` 子组件或使用 `v-if` 切换）
- 降级时 `console.warn` 记录日志，便于排查

### 5.5 预览模式支持

首页和 CMS 通用页面都支持预览模式：

```typescript
// 从页面 query 中获取 preview_token
const previewToken = ref<string | undefined>(undefined)

onLoad((options) => {
  previewToken.value = options?.preview_token
  loadPageConfig()
})

async function loadPageConfig() {
  // 预览模式：直接请求 API，不走缓存
  if (previewToken.value) {
    const data = await getCmsPage('home', previewToken.value)
    pageConfig.value = data.config
    return
  }
  // 正常模式：缓存优先 + 静默刷新
  // ...
}
```

### 5.6 首页改造后的组件结构

```vue
<template>
  <view class="page-index">
    <!-- CMS 动态渲染模式 -->
    <template v-if="renderMode === 'cms'">
      <!-- 保留原有品牌头部（搜索框等不由 CMS 控制的固定元素） -->
      <view class="hero-header">
        <!-- ...与现有相同... -->
      </view>

      <!-- CMS 渲染引擎 -->
      <CmsRenderer :config="pageConfig" />
    </template>

    <!-- 内置默认首页（L3 降级） -->
    <template v-else-if="renderMode === 'default'">
      <DefaultHomePage />
    </template>

    <!-- 加载骨架屏 -->
    <template v-else>
      <HomePageSkeleton />
    </template>

    <view class="safe-bottom" style="height: 40rpx" />
  </view>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { onLoad, onPullDownRefresh, onShareAppMessage } from '@dcloudio/uni-app'
import CmsRenderer from '@/components/cms/CmsRenderer.vue'
import { getCmsPage, checkCmsPageVersion } from '@/api/cms'
import { getCmsPageCache, setCmsPageCache } from '@/api/cms'
import type { CmsPageConfig } from '@/types/cms'
import { currentSite } from '@/config/sites'

type RenderMode = 'loading' | 'cms' | 'default'

const renderMode = ref<RenderMode>('loading')
const pageConfig = ref<CmsPageConfig | null>(null)
const previewToken = ref<string | undefined>(undefined)
const cachedVersion = ref<number>(0)

onLoad((options) => {
  previewToken.value = options?.preview_token
})

onMounted(() => {
  loadPageConfig()
})

/** 应用 page_settings 中的导航栏颜色配置 */
function applyNavigationBarColor(config: CmsPageConfig | null) {
  const settings = config?.page_settings
  if (settings?.title_bar_color) {
    uni.setNavigationBarColor({
      frontColor: settings.title_bar_text_color === '#000000' ? '#000000' : '#ffffff',
      backgroundColor: settings.title_bar_color,
      animation: { duration: 300, timingFunc: 'easeIn' },
    })
  }
}

async function loadPageConfig() {
  const PAGE_CODE = 'home'

  // 预览模式 — 直接请求，不走缓存
  if (previewToken.value) {
    try {
      const data = await getCmsPage(PAGE_CODE, previewToken.value)
      pageConfig.value = data.config
      renderMode.value = 'cms'
      applyNavigationBarColor(data.config)
    } catch {
      renderMode.value = 'default'
    }
    return
  }

  // 1. 尝试读取本地缓存
  const cache = getCmsPageCache(PAGE_CODE)
  if (cache) {
    pageConfig.value = cache.config
    cachedVersion.value = cache.version
    renderMode.value = 'cms'
    // 应用缓存中的导航栏颜色
    applyNavigationBarColor(cache.config)
  }

  // 2. 静默刷新 — 有缓存时先调用轻量 check 接口判断是否有更新，无缓存时直接拉取完整配置
  try {
    if (cache) {
      // 有缓存：先用 check 接口判断版本，避免每次传输完整配置 JSON
      const checkResult = await checkCmsPageVersion(PAGE_CODE, cachedVersion.value)
      if (!checkResult.has_update) {
        // 版本未变化，跳过完整请求
        return
      }
    }
    // 有更新或无缓存：请求完整配置
    const data = await getCmsPage(PAGE_CODE)
    pageConfig.value = data.config
    renderMode.value = 'cms'
    setCmsPageCache(PAGE_CODE, data)
    cachedVersion.value = data.version
    // 动态更新导航栏标题
    if (data.title) {
      uni.setNavigationBarTitle({ title: data.title })
    }
    // 应用导航栏颜色设置
    applyNavigationBarColor(data.config)
  } catch (err) {
    console.warn('[首页] CMS 配置加载失败:', err)
    // 无缓存时降级到默认首页
    if (!cache) {
      renderMode.value = 'default'
    }
    // 有缓存时保持缓存渲染（已在上方设置）
  }
}

onPullDownRefresh(async () => {
  await loadPageConfig()
  uni.stopPullDownRefresh()
})

onShareAppMessage(() => ({
  title: currentSite.shareTitle,
  path: '/pages/index/index',
}))
</script>
```

**DefaultHomePage 子组件**：将现有首页的硬编码内容（banner、分类导航、天气、推荐商品）抽取到 `components/default-home-page/index.vue`，与现有 `pages/index/index.vue` 逻辑一致，作为降级方案。

---

## 6. CMS 通用页面容器 — `pages/cms-page/index.vue`

用于加载活动页、促销页等非首页的 CMS 配置页面。

### 6.1 页面设计

```vue
<template>
  <view class="cms-page">
    <!-- 加载中 -->
    <view v-if="loading" class="cms-page__loading">
      <view class="cms-page__skeleton" v-for="i in 5" :key="i">
        <view class="skeleton-block" :style="{ height: (i % 2 === 0 ? 200 : 300) + 'rpx' }" />
      </view>
    </view>

    <!-- CMS 渲染 -->
    <CmsRenderer v-else-if="pageConfig" :config="pageConfig" />

    <!-- 加载失败 -->
    <view v-else class="cms-page__error">
      <empty-state
        icon="📄"
        title="页面加载失败"
        description="请检查网络后重试"
      />
      <view class="cms-page__retry" @tap="loadPageConfig">
        <text>点击重试</text>
      </view>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { onLoad, onPullDownRefresh, onShareAppMessage } from '@dcloudio/uni-app'
import CmsRenderer from '@/components/cms/CmsRenderer.vue'
import { getCmsPage, checkCmsPageVersion } from '@/api/cms'
import { getCmsPageCache, setCmsPageCache } from '@/api/cms'
import type { CmsPageConfig } from '@/types/cms'
import { currentSite } from '@/config/sites'

const pageCode = ref('')
const pageTitle = ref('')
const previewToken = ref<string | undefined>(undefined)
const pageConfig = ref<CmsPageConfig | null>(null)
const loading = ref(true)
const cachedVersion = ref<number>(0)

onLoad((options) => {
  pageCode.value = options?.page_code || ''
  previewToken.value = options?.preview_token

  if (!pageCode.value) {
    loading.value = false
    return
  }

  loadPageConfig()
})

/** 应用 page_settings 中的导航栏颜色配置 */
function applyNavigationBarColor(config: CmsPageConfig | null) {
  const settings = config?.page_settings
  if (settings?.title_bar_color) {
    uni.setNavigationBarColor({
      frontColor: settings.title_bar_text_color === '#000000' ? '#000000' : '#ffffff',
      backgroundColor: settings.title_bar_color,
      animation: { duration: 300, timingFunc: 'easeIn' },
    })
  }
}

async function loadPageConfig() {
  loading.value = true

  // 预览模式
  if (previewToken.value) {
    try {
      const data = await getCmsPage(pageCode.value, previewToken.value)
      pageConfig.value = data.config
      if (data.title) {
        pageTitle.value = data.title
        uni.setNavigationBarTitle({ title: data.title })
      }
      applyNavigationBarColor(data.config)
    } catch {
      pageConfig.value = null
    }
    loading.value = false
    return
  }

  // 先尝试缓存
  const cache = getCmsPageCache(pageCode.value)
  if (cache) {
    pageConfig.value = cache.config
    cachedVersion.value = cache.version
    if (cache.title) {
      pageTitle.value = cache.title
      uni.setNavigationBarTitle({ title: cache.title })
    }
    applyNavigationBarColor(cache.config)
    loading.value = false
  }

  // 静默刷新 — 有缓存时先调用轻量 check 接口，无缓存时直接拉取完整配置
  try {
    if (cache) {
      const checkResult = await checkCmsPageVersion(pageCode.value, cachedVersion.value)
      if (!checkResult.has_update) {
        // 版本未变化，跳过完整请求
        return
      }
    }
    // 有更新或无缓存：请求完整配置
    const data = await getCmsPage(pageCode.value)
    pageConfig.value = data.config
    cachedVersion.value = data.version
    setCmsPageCache(pageCode.value, data)
    if (data.title) {
      pageTitle.value = data.title
      uni.setNavigationBarTitle({ title: data.title })
    }
    applyNavigationBarColor(data.config)
  } catch (err) {
    console.warn(`[CmsPage] 页面 ${pageCode.value} 加载失败:`, err)
    if (!cache) {
      pageConfig.value = null
    }
  }
  loading.value = false
}

onPullDownRefresh(async () => {
  await loadPageConfig()
  uni.stopPullDownRefresh()
})

// M07: CMS 通用页面分享支持
onShareAppMessage(() => ({
  title: pageTitle.value || currentSite.shareTitle,
  path: `/pages/cms-page/index?page_code=${pageCode.value}`,
}))
</script>

<style lang="scss" scoped>
.cms-page {
  min-height: 100vh;
  background-color: var(--color-bg);

  &__loading {
    padding: var(--spacing-lg);
  }

  &__skeleton .skeleton-block {
    background: linear-gradient(110deg, var(--color-bg-light) 0%, var(--color-bg-warm) 50%, var(--color-bg-light) 100%);
    background-size: 300% 100%;
    animation: shimmer 2s infinite;
    border-radius: var(--radius-lg);
    margin-bottom: var(--spacing-md);
  }

  &__error {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-top: 200rpx;
  }

  &__retry {
    margin-top: 32rpx;
    padding: 16rpx 48rpx;
    background: var(--color-primary);
    border-radius: var(--radius-round);
    color: var(--color-text-white);
    font-size: var(--font-size-base);
  }
}

@keyframes shimmer {
  0% { background-position: 300% 0; }
  100% { background-position: -300% 0; }
}
</style>
```

### 6.2 页面 query 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page_code` | string | 是 | CMS 页面标识，如 `activity_spring_2026` |
| `preview_token` | string | 否 | 草稿预览 token（UUID），有此参数时加载草稿配置 |

**跳转示例**：
```typescript
// 从 CMS 链接跳转
uni.navigateTo({ url: '/pages/cms-page/index?page_code=activity_spring_2026' })

// 预览模式（管理后台扫码预览）
uni.navigateTo({ url: '/pages/cms-page/index?page_code=home&preview_token=a1b2c3d4-...' })
```

---

## 7. API 接口

### 7.1 需要对接的后端 API 列表

小程序端仅对接 **C端公开接口**（无需登录认证）：

| 接口 | 方法 | 路径 | 说明 | 认证 | 请求参数 |
|------|------|------|------|------|----------|
| 获取页面配置 | GET | `/api/v1/cms/pages/{page_code}` | 获取已发布的页面配置 JSON | 无 | `preview_token?` query 参数 |
| 检查版本更新 | GET | `/api/v1/cms/pages/{page_code}/check` | 对比版本号 | 无 | `version` query 参数 |

### 7.2 响应数据格式

> **⚠️ 重要约定**：CMS 所有接口统一使用现有 `{ code: 0, message: "success", data: {...} }` 响应格式，与 `utils/request.ts` 中 `responseData.code === 0` 的成功判断逻辑一致。**不使用** PRD 中提到的 `{ success: true, data: {...} }` 格式。后端实现时须遵循此约定，避免前端 request.ts 解析失败。

**GET /api/v1/cms/pages/{page_code}**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "page_code": "home",
    "title": "首页",
    "version": 5,
    "config": {
      "version": 1,
      "page_settings": {
        "background_color": "#faf6f0",
        "title_bar_color": "#2d4a3e",
        "title_bar_text_color": "#ffffff"
      },
      "components": [
        {
          "id": "comp_uuid_1",
          "type": "banner",
          "props": { ... },
          "style": { ... }
        }
      ]
    },
    "updated_at": "2026-03-31T10:00:00Z"
  }
}
```

**GET /api/v1/cms/pages/{page_code}/check?version=5**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "has_update": true,
    "version": 6
  }
}
```

### 7.3 请求头

所有 C 端请求自动携带 `X-Site-Id` header（由 `utils/request.ts` 统一注入），无需额外处理。

### 7.4 商品列表组件需要的额外 API

`CmsProductList` 组件需要调用已有的商品列表接口获取商品数据：

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 商品列表 | GET | `/api/v1/products` | 已有接口，支持 `category`、`ids`、`page_size` 等参数 |

### 7.5 优惠券组件需要的额外 API

`CmsCoupon` 组件需要调用优惠券相关接口：

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 优惠券列表 | GET | `/api/v1/coupons` | 按 ID 查询优惠券详情 |
| 领取优惠券 | POST | `/api/v1/coupons/{id}/claim` | 用户领券 |

> 如优惠券模块尚未实现，`CmsCoupon` 组件可先预留，加载失败时静默隐藏。

---

## 8. 样式规范

### 8.1 「野奢」设计系统 CSS 变量速查

以下变量已在 `uni.scss` 中全局定义，所有 CMS 组件必须复用这些变量，**禁止硬编码颜色值**：

| 变量 | 值 | 用途 |
|------|-----|------|
| `--color-primary` | `#2d4a3e` | 深苔绿主色 — 导航栏、按钮、标题强调 |
| `--color-primary-light` | `#3d6b5a` | 主色浅色 — 渐变终点、hover 态 |
| `--color-primary-lighter` | `#6b9b8a` | 主色更浅 — 辅助装饰 |
| `--color-primary-bg` | `rgba(45, 74, 62, 0.06)` | 主色背景 — 浅底色区域 |
| `--color-accent` | `#c8a872` | 暖铜金 — 价格、强调、指示器活跃态 |
| `--color-accent-light` | `#dbc49e` | 铜金浅色 — 渐变 |
| `--color-accent-bg` | `rgba(200, 168, 114, 0.08)` | 铜金底色 — 公告栏、标签背景 |
| `--color-bg` | `#faf6f0` | 暖沙页面底色 |
| `--color-bg-card` | `#ffffff` | 卡片白底 |
| `--color-bg-light` | `#f3ede4` | 浅暖灰底 |
| `--color-bg-warm` | `#f8f2e8` | 温暖底色 |
| `--color-text` | `#2a2520` | 正文深炭色 |
| `--color-text-secondary` | `#7a746c` | 副文字 |
| `--color-text-placeholder` | `#b5aea4` | 占位符 |
| `--color-text-white` | `#fffefa` | 白色文字（微暖） |
| `--color-red` | `#c45c4a` | 错误/删除 |
| `--color-orange` | `#d4804a` | 秒杀/限时 |
| `--radius-sm` | `10rpx` | 小圆角 |
| `--radius-md` | `16rpx` | 中圆角 |
| `--radius-lg` | `24rpx` | 大圆角 — 卡片、图片 |
| `--radius-xl` | `32rpx` | 特大圆角 — banner |
| `--radius-round` | `999rpx` | 胶囊 |
| `--shadow-sm` | `0 2rpx 12rpx rgba(42,37,32,0.04)` | 轻柔阴影 |
| `--shadow-md` | `0 6rpx 24rpx rgba(42,37,32,0.06)` | 中阴影 — 卡片 |
| `--shadow-glow` | `0 4rpx 20rpx rgba(200,168,114,0.15)` | 铜金发光 — 按钮 |
| `--spacing-xs` | `8rpx` | — |
| `--spacing-sm` | `12rpx` | — |
| `--spacing-md` | `20rpx` | — |
| `--spacing-lg` | `28rpx` | 页面两侧默认边距 |
| `--spacing-xl` | `40rpx` | — |
| `--ease-out-expo` | `cubic-bezier(0.19,1,0.22,1)` | 流畅缓动 |
| `--transition-base` | `all 0.35s ...` | 基础过渡 |

### 8.2 CMS 组件样式约定

1. **统一侧边距**：组件默认左右 padding 使用 `var(--spacing-lg)`（28rpx），与现有首页风格一致
2. **卡片风格**：需要背景的组件统一使用 `var(--color-bg-card)` + `var(--shadow-sm)` + `border: 1rpx solid rgba(42,37,32,0.04)`
3. **圆角层级**：外层容器 `--radius-xl`(32rpx)，内层元素 `--radius-lg`(24rpx)，小元素 `--radius-md`(16rpx)
4. **色彩禁忌**：禁止使用纯黑 `#000` 和纯白 `#fff`，使用 `--color-text`(`#2a2520`) 和 `--color-text-white`(`#fffefa`)
5. **间距呼吸感**：组件间最小间距 `10rpx`，推荐 `20rpx`，留白是「野奢」风格核心
6. **按压反馈**：所有可点击元素添加 `&:active { transform: scale(0.96); opacity: 0.85; }` 或 `scale(0.92)`
7. **骨架屏**：加载态使用 `shimmer` 动画（已在首页定义），底色为 `--color-bg-light` → `--color-bg-warm` 渐变

---

## 9. 开发任务清单

按开发顺序排列，建议严格按此顺序开发（有依赖关系）：

| 序号 | 任务 | 文件 | 依赖 | 估时 |
|------|------|------|------|------|
| 1 | 定义 CMS TypeScript 类型 | `types/cms.ts` | 无 | 0.5h |
| 2 | 实现超链接跳转工具 | `utils/cms-link.ts` | #1 | 1h |
| 3 | 封装 CMS API 接口 + 缓存工具 | `api/cms.ts` | #1 | 1h |
| 4 | 实现 CmsSpacer 组件 | `components/cms/CmsSpacer.vue` | #1 | 0.5h |
| 5 | 实现 CmsDivider 组件 | `components/cms/CmsDivider.vue` | #1 | 0.5h |
| 6 | 实现 CmsBanner 组件 | `components/cms/CmsBanner.vue` | #1, #2 | 2h |
| 7 | 实现 CmsImage 组件 | `components/cms/CmsImage.vue` | #1, #2 | 1.5h |
| 8 | 实现 CmsImageText 组件 | `components/cms/CmsImageText.vue` | #1, #2 | 1.5h |
| 9 | 实现 CmsNotice 组件 | `components/cms/CmsNotice.vue` | #1, #2 | 1.5h |
| 10 | 实现 CmsNav 组件 | `components/cms/CmsNav.vue` | #1, #2 | 1.5h |
| 11 | 实现 CmsRichText 组件 | `components/cms/CmsRichText.vue` | #1 | 1h |
| 12 | 实现 CmsVideo 组件 | `components/cms/CmsVideo.vue` | #1 | 1h |
| 13 | 实现 CmsProductList 组件 | `components/cms/CmsProductList.vue` | #1, #2 | 2.5h |
| 14 | 实现 CmsCoupon 组件 | `components/cms/CmsCoupon.vue` | #1, #2 | 2h |
| 15 | 实现组件注册表 | `components/cms/registry.ts` | #4-#14 | 0.5h |
| 16 | 实现 CmsRenderer 渲染引擎 | `components/cms/CmsRenderer.vue` | #15 | 2h |
| 17 | 抽取 DefaultHomePage 子组件 | `components/default-home-page/index.vue` | 无 | 1h |
| 18 | 首页改造（CMS 动态渲染 + 降级） | `pages/index/index.vue` | #3, #16, #17 | 3h |
| 19 | 新增 CMS 通用页面容器 | `pages/cms-page/index.vue` | #3, #16 | 2h |
| 20 | 更新 pages.json 路由配置 | `pages.json` | #19 | 0.5h |
| 21 | 联调测试 — 各组件渲染正确性 | — | 后端 API 就绪 | 3h |
| 22 | 联调测试 — 缓存与降级验证 | — | #21 | 1.5h |
| 23 | 联调测试 — 超链接跳转全流程 | — | #21 | 1.5h |
| 24 | 联调测试 — 预览模式 | — | #21 | 1h |
| 25 | 样式走查 & 多机型适配 | — | #21 | 2h |

**总估时**：约 **32 小时**（4 个工作日）

### 开发阶段建议

| 阶段 | 任务 | 天数 |
|------|------|------|
| Day 1 | #1-#5 类型定义 + 工具函数 + 简单组件 | 1天 |
| Day 2 | #6-#12 核心展示组件 | 1天 |
| Day 3 | #13-#19 动态数据组件 + 渲染引擎 + 页面改造 | 1天 |
| Day 4 | #20-#25 路由 + 联调测试 + 适配优化 | 1天 |

> ⚠️ Day 3-4 的联调测试依赖后端 CMS API 就绪。如后端未完成，可先使用 mock 数据开发（在 `api/cms.ts` 中返回硬编码 JSON）。

---

## 变更记录

| 日期 | 修订内容 | 对应评审编号 |
|------|----------|-------------|
| 2026-04-01 | API 响应格式统一为 `{ code: 0, message: "success", data }` ，与 request.ts 一致；响应示例中 `"ok"` 改为 `"success"`；check 接口响应补充 `message` 字段 | C01 |
| 2026-04-01 | 所有 11 个子组件 Props 接口中 `props` 重命名为 `data`；CmsRenderer 传递改为 `:data="comp.props"`；模板中 `props.xxx` 引用改为 `data.xxx` | C02 |
| 2026-04-01 | 首页和 CMS 通用容器的静默刷新改为先调用 `checkCmsPageVersion` 轻量接口，仅有更新时才请求完整配置 | H01 |
| 2026-04-01 | CmsCoupon 组件补充未登录领券跳转登录页逻辑；补充已领取/已抢光/未开始/已过期等失败状态 UI | H02 |
| 2026-04-01 | 首页和 CMS 通用容器 `loadPageConfig` 成功后调用 `uni.setNavigationBarColor()` 应用导航栏颜色 | H03 |
| 2026-04-01 | CmsRenderer 添加 `onErrorCaptured` 错误边界，单个组件异常不影响整体渲染 | M01 |
| 2026-04-01 | H5 链接 webview 跳转增加 `title` 参数支持动态标题，注释说明后续可新增通用 webview 页面 | M02 |
| 2026-04-01 | 补充 cms-page 分包规划建议（subPackages），评估主包体积影响 | M05 |
| 2026-04-01 | CMS 通用页面容器补充 `onShareAppMessage`，分享路径携带 `page_code` 参数 | M07 |
