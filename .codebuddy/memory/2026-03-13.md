# 2026-03-13 工作日志

1. **修复营位管理 500 错误**（`server/routers/campsites.py`）：
   - **路由顺序问题**：`/stats/overview` 放在文件末尾，被 `/{campsite_id}` 路径参数拦截（`stats` 被当成 campsite_id）。修复：将 stats/overview 移到文件顶部，在所有 `/{campsite_id}` 路由之前定义
   - **Inventory 字段名不匹配**：代码用了 `total_stock`/`locked_stock`/`sold_stock`，实际模型字段是 `total`/`locked`/`sold`。全文修正
   - **PricingRule 无 priority 字段**：代码引用了不存在的 `priority` 字段（查询排序、创建赋值、返回字段），全部移除
   - **DiscountRule 字段名错误**：代码用了 `name`/`conditions`/`discount_value`/`discount_type`，实际字段是 `rule_type`/`threshold`/`discount_rate`/`status`
   - **Product 模型字段不存在**：详情接口引用了 `require_identity`/`identity_mode`/`identity_fields`/`seckill_start_time`，这些字段在 Product 模型中不存在，替换为实际存在的字段
   - **清理未使用导入**：移除 `List`/`and_`/`PaginatedResponse`

2. **新增年卡配置和积分兑换配置管理 API**（`server/routers/admin.py`）：
   - 前端 `annual-cards.vue` 页面请求 `/admin/annual-card-configs` 和 `/admin/points-exchange-configs` 返回 404，原因是后端缺少这些接口
   - 新增 6 个接口：
     - `GET /api/v1/admin/annual-card-configs` — 年卡配置列表
     - `POST /api/v1/admin/annual-card-configs` — 创建年卡配置
     - `PUT /api/v1/admin/annual-card-configs/{config_id}` — 更新年卡配置
     - `GET /api/v1/admin/points-exchange-configs` — 积分兑换配置列表
     - `POST /api/v1/admin/points-exchange-configs` — 创建积分兑换配置
     - `PUT /api/v1/admin/points-exchange-configs/{config_id}` — 更新积分兑换配置
   - 注意字段映射：模型 `card_name` ↔ 前端 `name`，模型 `points_required` ↔ 前端 `required_points`，模型 `stock_used` ↔ 前端 `exchanged_count`

3. **新增次数卡配置和激活码管理 API**（`server/routers/admin.py`）：
   - 前端 `times-cards.vue` 页面请求 `/admin/times-card-configs` 和 `/admin/activation-codes` 返回 404
   - 新增 5 个接口：
     - `GET /api/v1/admin/times-card-configs` — 次数卡配置列表
     - `POST /api/v1/admin/times-card-configs` — 创建次数卡配置
     - `PUT /api/v1/admin/times-card-configs/{config_id}` — 更新次数卡配置
     - `GET /api/v1/admin/activation-codes` — 激活码列表（分页，支持 batch_no/status 过滤）
     - `POST /api/v1/admin/activation-codes/generate` — 批量生成激活码（16位大写字母+数字）
   - 字段映射：模型 `card_name` ↔ 前端 `name`

4. **修复财务管理页面 404 错误**（`server/routers/finance.py`）：
   - 前端请求 `/admin/finance/overview` 和 `/admin/finance/transactions`，后端路由端点名是 `/accounts` 不是 `/overview`
   - 将 `@router.get("/accounts")` 改为 `@router.get("/overview")`，同步更新函数名和文档注释

5. **新增报表管理 API**（新文件 `server/routers/reports.py`）：
   - 前端 `reports/index.vue` 请求 `/admin/reports/sales` 返回 404，原因是后端完全没有报表路由文件
   - 新建 `server/routers/reports.py`，实现 4 个接口：
     - `GET /api/v1/admin/reports/sales` — 销售报表（支持 day/week/month 粒度，返回 summary + details）
     - `GET /api/v1/admin/reports/users` — 用户分析（新增用户+活跃用户趋势）
     - `GET /api/v1/admin/reports/products` — 商品排行（按销量或收入排序，TOP 20）
     - `POST /api/v1/admin/reports/export` — 报表导出（占位，后续对接 Excel 生成）
   - 在 `server/routers/__init__.py` 和 `server/main.py` 中注册路由

6. **全面补全管理后台所有缺失 API**（`server/routers/admin.py`）：
   - 全面扫描前端 8 个 API 文件（98 个 admin 路径）对比后端已注册路由，一次性补全所有缺失接口
   - 新增模块涵盖：
     - **员工管理**：CRUD（GET/POST/PUT/DELETE /staff）
     - **角色权限**：角色列表、权限查询/更新
     - **操作日志**：新增 `/operation-logs` 路径兼容前端（原有 `/logs` 保留）
     - **FAQ管理（管理端CRUD）**：分类+条目的增删改查
     - **客服配置**：PUT /customer-service/config
     - **页面配置**：列表/详情/更新
     - **系统设置**：GET/PUT /settings
     - **免责声明管理**：模板列表+更新
     - **通知管理（管理端）**：模板列表、发送记录、统计
     - **二次确认**：验证码和操作密码
     - **会员详情 & 积分调整**
     - **年卡列表**
     - **次数卡消费规则 & 次数卡列表/调整**
     - **激活码导出**
     - **商品定价规则 CRUD**
     - **商品优惠规则 CRUD**
     - **库存管理**：列表/更新/批量开放
     - **押金记录**：占位接口
     - **订单退款审批**（兼容前端 /refund/approve 路径）
     - **部分退款**
     - **商品删除**
     - **管理端订单详情**
     - **商品状态更新（PUT方法兼容）**
   - 最终：前端 98 个 admin API → 后端 106 个路由，100% 覆盖

7. **小程序端全面检查 — 修复图片加载500错误、登录API不匹配、Banner数据格式**：
   - **问题1：图片500错误**：
     - 种子数据中图片路径是相对路径 `/images/rental-tent.jpg`，后端未配置静态文件服务
     - 修复：`server/main.py` 添加 `StaticFiles` 挂载 `/images` 目录
     - 创建 `server/images/` 目录 + 21个占位图片文件
     - 小程序端 `<image>` 标签需要完整URL，新增 `resolveImageUrl()` 工具函数（`miniprogram/utils/request.ts`）
     - 在首页、分类页、详情页的 `mapProduct` 函数中使用 `resolveImageUrl` 处理所有图片路径
   - **问题2：登录API路径不匹配**：
     - 前端调用 `POST /auth/login`，后端只有 `/auth/wx-login` → 添加 `/auth/login` 别名路由
     - 前端调用 `POST /auth/phone-login`，后端完全没有 → 新增 `PhoneLoginRequest` schema + `/auth/phone-login` 路由
   - **问题3：Banner接口数据格式不匹配**：
     - 前端期望 `data` 直接是 `{ banners: [...] }`，后端返回 `{ page_key, config_data: { banners: [...] } }`
     - 修复：`content.py` 的 `/pages/{page_code}` 接口直接返回 `config_data` 内容
   - **小程序端API覆盖检查结论**：
     - 已接入后端的API（4个）：`GET /products`、`GET /products/{id}`、`GET /pages/home_banner`、`POST /auth/login` + `POST /auth/phone-login` + `POST /auth/refresh` — 全部匹配 ✅
     - 大部分页面（cart、order、mine、address、identity、member、ticket、order-detail）仍使用 Mock 数据，尚未接入后端API

8. **修复管理后台营地日历页面 TypeError**（`admin/src/views/calendar/index.vue`）：
   - 错误：`calendarData.value.forEach is not a function`（第74行）
   - 根因：后端 `/admin/calendar` 返回 `{ date_start, date_end, products, cells }` 对象，前端直接赋值 `res.data` 给数组类型的 `calendarData`，导致 `.forEach()` 在非数组上调用
   - 字段不匹配：后端 cells 用 `total/available/sold/locked`，前端 CalendarItem 期望 `total_stock/available_stock/booked_stock`；后端 cells 无 `product_name`，需从 products 数组关联
   - 修复：`fetchData()` 中正确解析 `raw.cells` 数组 + 从 `raw.products` 映射商品名 + 字段名转换

9. **营地日历页面 UI 大幅优化**（`admin/src/views/calendar/index.vue`）：
   - **工具栏重做**：添加月份前后翻页按钮组 + "今天"快捷按钮 + 月份标签
   - **图例前置**：从底部移到顶部，用户一眼能看懂颜色含义
   - **格子尺寸放大**：min-width 72px → 80px，min-height 56px，数字字号 13px → 15px
   - **库存数字分层**：可用库存加粗大号 + 总库存灰色小号，用 `/` 分隔符，视觉重点突出
   - **状态色彩对比度增强**：可用（绿）/ 紧张（橙）/ 售罄（红）对应颜色同时应用到数字，不仅仅是背景
   - **今天列标记**：蓝色圆形日期 + 蓝色左边框贯穿整列
   - **周末列区分**：橙色暖调头部 + 微调数据格子背景
   - **斑马行**：隔行微色差增加行可读性
   - **商品名称列**：加宽到 160px，sticky 定位右边双线分隔，ellipsis 超长截断 + title tooltip
   - **表格外框**：圆角 border + 美化横向滚动条
   - **hover 交互**：格子悬浮微放大 + 阴影效果

10. **后端多营地（Multi-Site）支持完善**：
    - `server/services/auth_service.py`：
      - 添加 `import json`（`json.loads(settings.WECHAT_APPS)` 需要）
      - `wx_login()` 新增 `site_id: int = 1` 参数，传递给 `_code2session(code, site_id)`
      - `_code2session()` 根据 `site_id` 从 `WECHAT_APPS` JSON 查找对应 appid/secret
    - `server/routers/auth.py`：
      - 3 个登录路由（`/wx-login`、`/login`、`/phone-login`）均添加 `request: Request` 参数
      - 通过 `get_site_id(request)` 从 `X-Site-Id` 请求头提取营地 ID
      - 将 `site_id` 传递给 `auth_service.wx_login()`
    - 全部文件 lint 检查通过，0 错误

11. **uni-app 项目依赖修复与构建验证**：
    - `package.json` dcloudio 版本号修正：`3.0.0-4060620250520001`（不存在） → `3.0.0-alpha-5000320260310001`（vue3 latest）
    - 移除不存在的 `@dcloudio/uni-app-harmony` 依赖
    - vite 版本从 `^5.4.9` 固定为 `5.2.8`（匹配 `@dcloudio/vite-plugin-uni` peer 依赖）
    - `npm install` 成功（441 packages）
    - ✅ `npx uni build -p mp-weixin --mode xijiao` — 构建成功
    - ✅ `npx uni build -p mp-weixin --mode dalonggu` — 构建成功
    - 输出目录：`dist/build/mp-weixin`，可直接导入微信开发者工具运行

12. **uni-app 13个页面 Mock 数据全部替换为真实后端 API 调用**：
    - 使用项目已有的 `@/utils/request` 工具（`get/post/put/del`），所有 API 返回已由 request.ts 自动解包 `responseData.data`
    - 需要登录态的页面使用 `ensureLogin()` 确保登录
    - 所有 API 调用加 try-catch，失败时 `uni.showToast` 提示
    - 具体修改页面：
      - **cart/index.vue**：`GET /cart/` 列表、`PUT /cart/items/{id}` 改数量、`DELETE /cart/items/{id}` 删除，图片用 `resolveImageUrl` 处理
      - **order-confirm/index.vue**：`GET /products/{id}` 商品信息、`GET /users/identities` 出行人列表、`POST /orders` 创建订单
      - **payment/index.vue**：`POST /orders/{id}/mock-pay` 模拟支付（success/fail）
      - **order/index.vue**：`GET /orders` 分页列表（支持 status 筛选）、`POST /orders/{id}/cancel` 取消、`POST /orders/{id}/refund` 退款
      - **order-detail/index.vue**：`GET /orders/{id}` 详情、`POST /orders/{id}/refund` 退款，状态步骤动态计算
      - **ticket/index.vue**：`GET /orders/{id}/tickets` 票列表、`POST /tickets/{id}/refresh-qr` 刷新 QR token
      - **address/index.vue**：`GET /users/addresses` 列表、`DELETE /users/addresses/{id}` 删除
      - **identity/index.vue**：`GET /users/identities` 列表、`POST /users/identities` 添加、`PUT /users/identities/{id}` 更新、`DELETE /users/identities/{id}` 删除
      - **profile/index.vue**：`PUT /users/me` 更新用户信息
      - **member/index.vue**：`GET /members/annual-card`、`GET /members/times-cards`、`GET /members/points`、`POST /members/times-cards/activate`
      - **staff/index.vue**：`POST /staff/tickets/scan` 扫码验票
      - **customer-service/index.vue**：`GET /faq/categories` FAQ分类、`GET /faq/items?keyword=xxx` FAQ搜索
      - **mine/index.vue**：`GET /users/me` 刷新用户信息
    - **payment-result/index.vue** 纯展示页不修改
    - 全部文件 lint 0 错误 ✅

13. **后端所有路由添加 site_id 数据隔离**：
    - 为 8 个路由文件添加 `from middleware.site import get_site_id` 导入和 `request: Request` 参数
    - **products.py**：C端4个接口（list/detail/inventory/price-calendar）+ B端4个接口均添加 site_id 过滤/写入
    - **orders.py**：B端 `admin_list_orders` 按 `Order.site_id` 过滤（C端通过 user 已隔离，不改）
    - **content.py**：`list_faq_categories`/`search_faq_items`/`get_disclaimer`/`get_page_config` 均加 site_id
    - **finance.py**：`get_finance_overview`/`list_transactions` 传入 `site_id` 给 service 层（service 已支持）
    - **members.py**：`get_annual_card` 年卡配置列表按 `site_id` 过滤
    - **campsites.py**：全部 12 个接口（stats/list/detail/create/update/status/delete/inventory/batch-inv/pricing-rules CRUD）均加 site_id 过滤，创建时写入 site_id
    - **reports.py**：3个报表接口（sales/users/products）所有查询加 `Order.site_id`/`User.site_id` 过滤
    - **admin.py**：40+ 个接口全部添加 site_id 过滤/写入，覆盖 Dashboard（realtime/trends/ranking/member-stats/finance-summary/category-revenue/heatmap）、用户列表、会员管理、操作日志、营地日历、年卡配置CRUD、次数卡配置CRUD、激活码管理、FAQ管理CRUD、页面配置、系统设置、免责声明、订单管理、商品管理、库存管理等
    - notifications.py 不改（Notification 模型无 site_id，已通过 user_id 隔离）
    - 全部文件 lint 检查通过 ✅

14. **uni-app TypeScript 严格类型检查修复**：
    - `src/utils/auth.ts`：修复 `uni.login()` 返回类型不支持数组解构 `[err, res]` 的问题
      - `wxLogin()`：`const [err, res] = await uni.login({})` → `const loginRes = await uni.login({}) as unknown as [unknown, UniApp.LoginRes | undefined]` + 分别取 `loginRes[0]` / `loginRes[1]`
      - `phoneLogin()`：同样修复，变量名用 `loginRes2/err2/res2` 避免冲突，同时修复了原代码中 `res2` 变量未定义的 bug（原代码第47行引用了不存在的 `res2.code`）
    - `src/pages/category/index.vue`：`onSearchInput` 事件处理器类型从 `(e: { detail: { value: string } }) => void` 改为 `(e: any) => void`，解决 uni-app `<input>` 组件的 `@input` 事件类型与标准 DOM `InputEvent` 不兼容的问题
    - ✅ `npx vue-tsc --noEmit` — 0 错误
    - ✅ `npx uni build -p mp-weixin --mode xijiao` — 构建成功
    - ✅ `npx uni build -p mp-weixin --mode dalonggu` — 构建成功

15. **删除旧版 miniprogram 目录**：
    - `miniprogram/` 是迁移前的原生微信小程序代码（17个页面，wxml/wxss/ts 格式）
    - 已被 `uni-app/` 完全替代，安全删除
