# 2026-03-12 工作日志

## 项目：某露营地微信小程序

### 已完成

1. **架构文档v1.1修补完成**：修复评审发现的12个问题（3🔴+7🟡+2附加）
   - 错误码体系统一、营地日历API、消息管理B端API、XSS/CSRF防护等
   - 复审评分从8.7提升至9.3/10，通过正式基线

2. **后端项目初始化**：/Users/nathan/Projects/yyyl/server/
   - 45张SQLAlchemy模型（558字段、74索引、31枚举）
   - Alembic数据库迁移配置
   - 依赖安装（FastAPI, SQLAlchemy, asyncpg, Redis, jose, bcrypt, cryptography等）

3. **Pydantic Schemas开发**：12个Schema模块全部通过导入测试
   - 修复Pydantic v2兼容问题：移除`from __future__ import annotations`
   - 修复字段名`date`与`datetime.date`类型冲突（使用`DateField`别名）
   - 修复passlib与bcrypt 4.2不兼容（改用直接bcrypt库）

4. **认证与安全工具**：
   - JWT双Token（access 2h + refresh 7d）
   - AES-256-CBC加密/解密（身份证号等）
   - bcrypt密码哈希
   - 认证中间件（get_current_user, get_current_admin, require_role等）

5. **业务服务层**（7个服务模块）：
   - auth_service: 微信登录、管理员登录
   - product_service: 商品CRUD、价格日历
   - order_service: 下单、退票、支付、秒杀
   - inventory_service: 库存查询、锁定/释放、批量操作
   - member_service: 年卡、次数卡、积分
   - ticket_service: 电子票、扫码验票
   - finance_service: 财务账户、提现、押金

6. **路由层**（11个路由文件，71条API）：
   - auth, products, orders, cart, members, users, tickets, finance, admin, content, notifications
   - main.py注册所有路由，验证通过（76条总路由）

7. **微信小程序前端开发完成**：/Users/nathan/Projects/yyyl/miniprogram/（106个文件）
   - 17个页面：首页、分类、购物车、订单列表、我的、商品详情、订单确认、支付、支付结果、订单详情、电子票、会员中心、个人信息、身份管理、地址管理、员工验票、客服
   - 4个通用组件：product-card、countdown、empty-state、price-tag
   - 4个工具模块：request（网络请求）、auth（登录授权）、storage（本地存储）、util（通用工具）
   - 完整TypeScript类型定义、SCSS变量体系、户外露营自然风设计

8. **测试与合规检查报告完成**：docs/test_report.md
   - 综合评分 8.5/10
   - 41项检查：32✅通过 / 8🟡待完善 / 1🔴问题
   - 安全审计通过：SQL注入✅、XSS✅、AES加密✅、JWT认证✅
   - 发现31处TODO骨架代码（cart/content/notifications/admin 4个路由模块未接入Service）
   - 核心问题：订单定价规则未应用、退票校验缺失、购物车全部骨架代码

9. **Web管理后台前端开发完成**：/Users/nathan/Projects/yyyl/admin/
   - 技术栈：Vue 3 + TypeScript + Vite + Element Plus + Pinia + Vue Router 4 + ECharts + Axios
   - 构建验证通过（vite build成功，6.26s）
   - 项目结构：
     - `src/api/` — 7个API模块（auth, dashboard, product, order, member, finance, system）
     - `src/stores/` — 2个Pinia Store（user, app）
     - `src/router/` — 路由配置（含NProgress + 权限守卫）
     - `src/layout/` — 主布局（侧边栏 + 顶栏 + 面包屑）
     - `src/views/` — 15个页面视图：
       - login: 登录页（账号密码 + 微信扫码预留）
       - dashboard: 数据看板（实时卡片 + 趋势图 + 销售排行 + 品类饼图 + 会员数据 + 财务概览）
       - calendar: 营地日历（按月查看库存/价格矩阵）
       - products/index + edit: 商品管理（CRUD/上下架/定价）
       - orders/index + detail: 订单管理（筛选/详情/退款审批）
       - members/index + detail: 会员管理（搜索/详情/积分调整）
       - members/annual-cards: 年卡管理（配置/积分兑换）
       - members/times-cards: 次数卡管理（配置/激活码/消耗规则）
       - finance: 财务管理（概览/交易流水）
       - reports: 数据统计（销售报表/用户分析/商品排行+ECharts图表）
       - content/faq: FAQ管理（分类+条目CRUD）
       - content/pages: 页面编辑
       - content/notifications: 消息管理（模板/发送记录/统计）
       - system/staff: 员工管理
       - system/settings: 系统设置（基本/退票规则/客服/免责声明）
       - system/logs: 操作日志
     - `src/types/` — 完整TypeScript类型定义（涵盖所有后端实体）
     - `src/utils/` — 工具函数（request封装+Token管理+格式化）
     - `src/styles/` — Element Plus主题定制（露营自然风：#2E7D32主色）
   - 设计特色：
     - 暗色侧边栏 + 白色主区域，操作高效
     - 数据看板30秒自动刷新实时数据
     - 路由懒加载 + keep-alive缓存
     - CSRF防护（X-Request-Token请求头）
     - Token自动刷新 + 并发请求队列

10. **README.md 编写完成**：项目根目录 README.md
    - 包含：系统架构图、项目结构、环境要求、快速启动（后端/管理后台/小程序）、环境变量说明、常用命令速查、API模块概览、页面一览、安全机制、技术栈总结
    - 修复了 pyright 隐式导入报错（添加 pyrightconfig.json）

### 全部开发完成 ✅
- PRD v1.4 → 架构文档v1.1 → 后端45表+71API → 小程序17页面 → 测试报告 → Web管理后台15页面 → README

11. **Docker + K8s 部署方案完成**：
    - `server/Dockerfile` — Python 3.11-slim 多阶段构建，非root用户，健康检查
    - `server/.dockerignore` — 排除非必要文件
    - `admin/Dockerfile` — Node 构建 → Nginx 静态托管，多阶段构建
    - `admin/nginx.conf` — SPA路由支持 + API代理
    - `admin/.dockerignore`
    - `nginx/nginx.conf` — 网关层完整配置：反向代理、多级限流（秒杀1r/s、登录5r/m、普通API 10r/s）、gzip、安全头
    - `nginx/Dockerfile` — Nginx 1.25-alpine
    - `docker-compose.yml` — 6个服务编排：postgres + redis + api + celery-worker + celery-beat + admin + nginx
    - K8s 部署清单（`k8s/` 目录）：
      - `namespace.yaml` — yyyl 命名空间
      - `configmap.yaml` — 非敏感配置
      - `secret.yaml` — 敏感配置模板
      - `api-deployment.yaml` — FastAPI Deployment（就绪/存活/启动探针、资源限制、滚动更新）+ Service
      - `api-hpa.yaml` — HPA 自动扩缩容（2→8 Pod，CPU>60%/内存>75%触发，快扩慢缩）
      - `worker-deployment.yaml` — Celery Worker Deployment（2副本，exec探针）
      - `beat-deployment.yaml` — Celery Beat Deployment（单副本，Recreate策略）
      - `admin-deployment.yaml` — 管理后台 Deployment + Service
      - `ingress.yaml` — Nginx Ingress（SSL、限流注解、API+管理后台双域名）
    - 更新 `.gitignore`（排除 k8s/secret.yaml 和 ssl/）
    - 更新 `README.md`（项目结构新增 nginx/k8s/docker-compose，新增部署说明章节）
    - **部署策略**：开发→本地直接启动（简单直接）/ 集成测试→docker-compose / 线上→腾讯云TKE
    - **数据层建议**：K8s只跑无状态服务，PG/Redis用腾讯云托管（TDSQL-C + 云Redis）

12. **品牌名批量替换**：全项目"一月一露" → "某露营地"
    - 涉及 30+ 个文件，39 处替换
    - 覆盖范围：小程序（app.json/wxml/ts/json/config）、后端（main.py/config.py/Dockerfile）、管理后台（router/login/layout/styles/stores/utils）、Nginx、K8s、docker-compose、PRD、架构文档、测试报告、README

13. **Celery 异步任务模块完成**：
    - `celery_app.py` — Celery 实例初始化（Redis Broker、队列路由、autodiscover）
    - `celery_config.py` — 23 个 Beat 定时任务调度配置
    - `tasks/helpers.py` — 同步 DB/Redis（psycopg2）+ 任务监控装饰器
    - `tasks/inventory.py` — 定时开票、库存超时释放、库存一致性校验（3个任务）
    - `tasks/order.py` — 超时取消通知、自动完成+收入确认（2个任务）
    - `tasks/member.py` — 年卡/次数卡到期、到期提醒、积分过期（5个任务）
    - `tasks/finance.py` — 收入确认、押金超时提醒（2个任务）
    - `tasks/notification.py` — 行程提醒、活动开始提醒、退款审批超时（3个任务）
    - `tasks/stats.py` — Dashboard聚合、热力图、日/周/月报（5个任务）
    - `tasks/cleanup.py` — Token清理、验证码清理、日志归档（3个任务）
    - 新增依赖：`psycopg2-binary==2.9.9`（同步DB驱动）
    - 更新 `.env.example` 品牌名
    - 验证通过：23个Beat任务 + 23个任务函数全部注册成功

14. **品牌名 UI 可配置化**：
    - 小程序：新建 `miniprogram/config/brand.ts` 品牌配置（name/slogan/shareTitle/shareDefaultTitle）
      - `app.ts` — globalData 挂载 brandConfig
      - `pages/index/index.wxml` — 品牌名改为 `{{brandName}}` 数据绑定
      - `pages/index/index.ts` — import brandConfig，分享标题/data 从配置读取
      - `pages/product-detail/index.ts` — 分享兜底标题从配置读取
      - `pages/cart/index.ts` — mock 商品名用模板字符串拼接
      - `app.json` / `index.json` — navigationBarTitleText 改为"一月一露"（JSON 不支持动态化）
    - 管理后台：新建 `admin/src/config/brand.ts`
      - `views/login/index.vue` — 标题改为 `{{ brand.name }}`
      - `layout/index.vue` — 侧边栏 logo 文字改为 `{{ brand.name }}`
      - `router/index.ts` — document.title 改为 `brandConfig.name`
      - `index.html` — 静态标题改为"一月一露"
    - 后端：`config.py` APP_NAME 默认值改为"一月一露"，`.env.example` 同步
    - 配置文件暂时值：**一月一露**
    - 以后改品牌名只需修改 2 个配置文件 + 3 个 JSON/HTML（无法动态化的）

15. **ORM 模型修复 + 数据库建表**：
    - 修复 9 个 JSONB 字段的 `server_default` 多余引号（`"'[]'"` → `"[]"`），涉及 product.py/member.py/content.py/user.py
    - 修复 30 个 `_id` 字段缺失 `ForeignKey` 约束，涉及 6 个模型文件：
      - `user.py` — user_address.user_id, user_identity.user_id（+ForeignKey import）
      - `product.py` — 10 个字段（ext_camping/rental/activity/shop.product_id, sku.product_id, pricing_rule.product_id, discount_rule.product_id, inventory.product_id/sku_id, inventory_log.inventory_id）
      - `order.py` — 8 个字段（order.user_id/parent_order_id, order_item.order_id, cart.user_id, cart_item.cart_id/sku_id, ticket.order_id/order_item_id）
      - `member.py` — 8 个字段（annual_card.user_id/config_id, booking_record.annual_card_id, times_card.user_id/config_id, activation_code.config_id, consumption_rule.config_id, points_record.user_id）
      - `admin.py` — 2 个字段（admin_user.role_id, admin_permission.role_id）
      - `notification.py` — 1 个字段（notification.user_id）
      - `content.py` — 2 个字段（disclaimer_signature.template_id, faq_item.category_id）
    - Alembic 迁移成功：46 张表（45 业务 + 1 alembic_version）

16. **测试管理员账号创建**：
    - 创建种子脚本 `server/seed_admin.py`（使用原始 SQL 避免 ORM relationship 懒加载问题）
    - 初始化：super_admin 角色 + 52 项权限（13 资源 × 4 操作）+ admin 账号
    - **测试账号**：用户名 `admin`，密码 `admin123456`，角色 `super_admin`

17. **登录 404 修复 + 额外 ForeignKey 修复**：
    - 前端 API 路径错误：`/admin/auth/login` → `/auth/admin-login`（修改 `admin/src/api/auth.ts`）
    - 后端 `admin_login` 返回结构调整：扁平字段 → 嵌套 `user` 对象（匹配前端 `LoginResponse` 类型定义）
    - 修复遗漏的 ForeignKey（之前 replace_in_file 因重复匹配失败的）：
      - `order.py` — Order.user_id, Order.parent_order_id, CartItem.cart_id/sku_id, Ticket.order_id/order_item_id/user_id
      - `member.py` — AnnualCard.user_id/config_id, AnnualCardBookingRecord.annual_card_id, ActivationCode.config_id, PointsRecord.user_id
      - `product.py` — DiscountRule.product_id, Inventory.product_id/sku_id
    - 全部 mapper 配置验证通过，API 登录接口 200 OK
